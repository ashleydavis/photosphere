# CLI Move Tests

Manual test scripts for moving files between databases via the `psi` CLI.

## Tests

- [move-file-between-databases.md](move-file-between-databases.md) - Create two databases, import a file into the first, move it to the second, and verify
