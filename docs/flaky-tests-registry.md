# Flaky-test failure registry

A repo-wide record of every known intermittent (flaky) failure mode across the test suites in this repo (unit, CLI smoke, Electron smoke, mobile smoke, LAN-share, and any future suite). Each entry captures the mode's stable signature, which suite it came from, its root cause, and whether it is believed fixed. Match a failure's output against the patterns below, so a recurrence of a checked (fixed) mode is caught as a regression and its fix removed.

## Rules for categorizing a flaky failure

1. Only record a mode here if it is flaky: it has passed before and fails intermittently. A failure that happens every run is a deterministic bug, not a flaky mode. Fix it directly and do not add it here.
2. Fingerprint the failure by its earliest root error line, never by a downstream cascade. If one component never starts, a later "not ready" line is a symptom of the same mode, not a separate one.
3. Normalize out volatile tokens before matching. Strip ports, PIDs, timestamps, file paths, correlation/command ids, durations, attempt counts, and hashes. Keep only the invariant words.
4. One entry per distinct root cause. Same wording but a different root cause is a different entry. Different wording but the same root cause is one entry that lists both patterns.
5. Every entry carries: id, a Fixed checkbox, suite (the test suite and command it was seen in), pattern (a regex over the invariant text), fix commit, first seen, recurrences, root cause, and evidence.
6. A mode's Fixed box stays unchecked until a fix has landed AND a repeated run of the suite passes clean. Only then tick the box and record the fixing commit hash in Fix commit.
7. Recurrence rule: if a new failure matches the pattern of a checked entry, that fix is disproven. Untick the box, append the recurrence date, and remove the failed fix commit so a fresh fix can be tried. A checked box is only as good as the next matching failure.
8. The pattern must match invariant text only. Use `port \d+`, never `port 43227`.
9. Checked means the Fixed box is ticked AND Fix commit records the commit hash that fixed it. That commit is what to remove if the mode ever recurs.

## Parallel-only failure modes

Modes in this section are not flaky in the ordinary sense. The suite is sound when it is run on its own and fails only in company: two scripts running at the same time contend on a file, a port, a directory, a lock or a device, and one breaks the other. `bun run test:everything` starts the suites concurrently and is what `.githooks/pre-commit` runs, so a mode here refuses a commit with a failure that does not reproduce when the suite is rerun alone.

Entries here carry the same fields as the main registry plus two of their own:

- **Pair**: the two scripts that must run together to produce it. It may name the same script twice, which is a self-pair: the suite cannot be run twice at once, so a second worktree or a rerun started before the last one finished will break it.
- **Shared resource**: the path, port, lock or device they contend on.

Rules for this section, on top of the ones above:

1. A mode only belongs here when `bun run test:parallel` classified it `interference`, meaning both scripts passed alone and at least one failed when they were run together. A verdict of `inconclusive` is not evidence: it means a script was already unreliable on its own, and that belongs in the main registry.
2. A mode reproducible from a single script alone belongs in the main registry, not here, even if it was first noticed under the parallel runner.
3. Record the detecting session directory under `tmp/parallel-check/<timestamp>/` in Evidence, the same way the main registry records `find-flakey-tests` sessions.

`SHARE-DB-REVIEW-STEP-NEVER-REACHED` below is the shape a self-pair finding takes: its root cause names another worktree's Electron suite announcing on the LAN during this one's discovery window, which is one copy of `test:electron` breaking another. It is left in the main registry because it also reproduces from one suite alone, against unrelated LAN traffic.

All three entries here came out of the first run of `bun run test:parallel` over the fast set, and all three are self-pairs: `test`, `test:cli` and `test:electron` each failed against a second copy of itself, while the three cross pairs between them were all `ok`. That is what the check predicted: a suite that hardcodes a path is guaranteed to collide with a second copy of itself, and until then nothing looked.

Two of the three are now fixed by the per-test temporary directories on the `mobile` branch (`scripts/lib/allocate-test-temp-dir.sh`, sourced by both shell suites). Re-running the same check over the merged tree gives `ok` for all six combinations except `test` against itself, which is unchanged: the allocator is a shell helper and the surviving mode is inside a Jest test, so nothing reaches it. Session `tmp/parallel-check/20260805-062649`.

### UNIT-ENCRYPTION-TMP-DIR-SHARED

- [x] Fixed. Not yet 10x clean: 3 of 3 attempts of the self-pair pass after the fix (session `tmp/parallel-check/20260805-140525`) where 2 of 3 failed immediately before it (`tmp/parallel-check/20260805-134146`). Three attempts is enough to be meaningful here only because the failure rate was around two thirds; leave the caveat until a longer run backs it up.
- Fix commit: `packages/encryption/src/test/key-utils.test.ts` now calls `createTestTempDir('encryption-key-utils')` from `node-utils` instead of hardcoding `path.join(__dirname, '..', 'tmp')` (uncommitted working change; record the commit hash here once committed).
- Suite: unit tests (`bun run test`), `packages/encryption/src/test/key-utils.test.ts`
- Pair: `test` with `test` (a self-pair)
- Shared resource: `packages/encryption/src/tmp/`, a fixed path under the package rather than a per-run directory
- Pattern: `ENOENT: no such file or directory, open '.*/packages/encryption/src/tmp/test-private-only-loadEncryptionKeys\.key'`
- First seen: 2026-08-04, the first real run of `bun run test:parallel`, session `tmp/parallel-check/20260804-115249`. Both sides passed alone; side 1 failed when the suite ran against a second copy of itself.
- Recurrences:
  - 2026-08-05, session `tmp/parallel-check/20260805-134146`, three attempts of the self-pair: attempt 1 passed, attempts 2 and 3 failed, both with the identical ENOENT. **So this mode is intermittent, not deterministic**, unlike the two sibling entries, which is what you would expect from a race whose window is the gap between one process's `fs.mkdir` and its `fs.writeFile`. A single run is not enough to see it: an earlier run the same day that made one attempt came back `ok` and would have been read as a fix. `bun run test:parallel` now makes exactly one attempt per combination, so it would not have found this mode on its own, and a similar intermittent conflict in future will need `bun run find-flakey-tests` to surface it.
  - 2026-08-05, session `tmp/parallel-check/20260805-062649`, on the tree after the `mobile` per-test temporary directories landed. Byte-identical failure. **The two sibling entries above were fixed by that work and this one was not**, which is the useful thing this recurrence says: the same commit that fixed them added the TypeScript half of the allocator, `createTestTempDir` in `packages/node-utils/src/lib/test-temp-dir.ts`, and this test simply did not call it: it still resolved its directory as `path.join(__dirname, '..', 'tmp')`. The mechanism reached Jest all along, through `getProcessTmpDir()` reading `TEST_TMP_DIR`; the test was bypassing it. That is what the fix above corrects.
- Root cause: **the test suite deletes a fixed directory that another copy of itself is writing into.** `key-utils.test.ts` puts its generated key files in `path.join(__dirname, '..', 'tmp')`, which is the same absolute path for every process on the machine, and its `afterAll` does `fs.rm(testKeysDir, { recursive: true, force: true })`. `createTestKeyPath` calls `fs.mkdir(testKeysDir, { recursive: true })` and returns, and the caller then calls `fs.writeFile`. When a second run's `afterAll` removes the directory inside that window, the write has no parent directory and fails with ENOENT. Run on its own the suite never sees this, because only one process ever owns the directory.
- Evidence: session `tmp/parallel-check/20260804-115249`, `pair-test-test-side1-1.log` at the `encryption test:` lines: `FAIL src/test/key-utils.test.ts`, `1 failed, 34 passed`, the ENOENT naming the exact path, and the stack pointing at `src/test/key-utils.test.ts:107:9`, which is the `await fs.writeFile(keyPath, privateKeyPem)` two lines after the `mkdir`. Side 2 of the same attempt exited 0, and `alone-test-1.exit` records 0, so the suite is sound on its own.

### CLI-SUITE-FIXED-TEST-TMP-DIR

- [x] Fixed. Not yet 10x clean: one re-run of `bun run test:parallel` over the fast set on the merged tree gives `ok` for this combination where it gave `interference` before, session `tmp/parallel-check/20260805-062649`. One clean pair is evidence the cause is gone, not that the mode is retired, because the original failure was deterministic rather than rare. Leave the box ticked and the caveat here until a repeated run backs it up.
- Fix commit: per-test temporary directories on the `mobile` branch, commit 309dfdbb "Gave every test its own temporary directory". `apps/cli/smoke-tests.sh` now sources `scripts/lib/allocate-test-temp-dir.sh` and each test is handed a uniquely named directory, so two runs out of one checkout no longer land on the same path.
- Suite: CLI smoke tests (`bun run test:cli`), many tests rather than one. 25 of 65 failed on one side and 13 of 65 on the other in the same attempt.
- Pair: `test:cli` with `test:cli` (a self-pair)
- Shared resource: `apps/cli/test/tmp/<test-name>/`, the per-test working directory, which is a fixed path unless `TEST_TMP_DIR` is set
- Pattern: `The directory .* is not empty or already contains a database`
- Cascade symptom (same mode, not a new entry): `Second identical sync early-outs \(lastSyncedAt unchanged\): expected .*, got $`, and any later assertion in a test whose database was created by the other run
- First seen: 2026-08-04, the first real run of `bun run test:parallel`, session `tmp/parallel-check/20260804-115249`. Both sides passed alone; both sides failed when the suite ran against a second copy of itself.
- Recurrences: none yet
- Root cause: **two runs of the suite use the same per-test directory, so each finds the other's database already there.** Each test works in `apps/cli/test/tmp/<test-name>/<n>/`, derived from the test's own name and so identical between runs. `psi init` refuses a directory that already holds a database, which is correct behaviour, so whichever run arrives second fails at its first step and then fails every assertion that depended on it. The suite already carries the mechanism to avoid this and does not switch it on: `apps/cli/smoke-tests.sh:56` carries the comment "Override TEST_TMP_DIR to run tests in parallel", and line 57 then defaults it to the one fixed path `$_CLI_ABS_DIR/test/tmp`. The vault and config directories are derived from it too (lines 64 and 65), so two runs also share those. This is the same variable as in CLI-SUITE-WIPED-SHARED-TEMP-DIR below, which is a different mode with the same cause underneath: one fixed directory standing in for what should be per-run state.
- Evidence: session `tmp/parallel-check/20260804-115249`, `pair-test-cli-test-cli-side1-1.log`: `bun run start -- init --db .../apps/cli/test/tmp/03-add-png/3/test-db --yes` reporting both `Created new media file database in ...` and `The directory ... is not empty or already contains a database.` in the same invocation, then `[FAIL] Initialize database (exit code: 1)`, ending `25 of 65 tests failed`. `pair-test-cli-test-cli-side2-1.log` ends `13 of 65 tests failed`. `alone-test-cli-1.exit` records 0, so the suite is sound on its own.

### ELECTRON-SUITE-FIXED-PER-TEST-TMP-DIR

- [x] Fixed. Not yet 10x clean: one re-run of `bun run test:parallel` over the fast set on the merged tree gives `ok` for this combination where it gave `interference` before, session `tmp/parallel-check/20260805-062649`. Same caveat as the CLI entry above, for the same reason.
- Fix commit: per-test temporary directories on the `mobile` branch, commit 309dfdbb "Gave every test its own temporary directory". `apps/desktop/smoke-tests.sh` now sources `scripts/lib/allocate-test-temp-dir.sh` instead of clearing and reusing `<test>/tmp`.
- Suite: Electron smoke tests (`bun run test:electron`), several tests rather than one. 5 of 25 failed on one side and 2 of 25 on the other in the same attempt: 3 (open-database), 5 (add-secret), 6 (add-database-entry), 15 (duplicate-name) and 16 (remove-recent-database) on side 1, and 3 and 5 on side 2.
- Pair: `test:electron` with `test:electron` (a self-pair)
- Shared resource: `apps/desktop/smoke-tests/<n>-<name>/tmp/`, the per-test working directory, a fixed path with no override
- Pattern: `The directory .*/apps/desktop/smoke-tests/.*/tmp/test-db is not empty or already contains a database`
- First seen: 2026-08-04, session `tmp/parallel-check/20260804-154758`. `test:electron` was recorded clean alone in two earlier sessions the same day (`20260804-115249` and `20260804-122925`); both sides failed when it ran against a second copy of itself.
- Recurrences: none yet
- Root cause: **two runs of the suite use one per-test directory and clear it out from under each other.** `apps/desktop/smoke-tests.sh:119-121` does `rm -rf "$dir/tmp"` then `mkdir -p "$dir/tmp"` before each test, where `$dir` is the test's own directory under `apps/desktop/smoke-tests/`, and each test then builds its fixture inside it (`3-open-database/test.sh:12` sets `TMP_DIR="$TEST_DIR/tmp"`). Nothing makes the path per run: there is no equivalent of the CLI suite's `TEST_TMP_DIR` here. Two runs therefore both delete and both populate the same directory, and the observed failure is the second half of that: `psi init` correctly refused a directory that already held the other run's database, so the fixture was never built and the test failed at 0s. The `rm -rf` half is the more dangerous one and has not yet been caught in the act.
- Evidence: session `tmp/parallel-check/20260804-154758`, `pair-test-electron-test-electron-side1-1.log`, test 3's own log showing `init --db .../3-open-database/tmp/test-db` answered with `The directory ... is not empty or already contains a database.`, then the test failing at 0s. Side 1 ends `5 of 25 tests failed`, side 2 `2 of 25 tests failed`, and both exit files record 1. This is the same shape as CLI-SUITE-FIXED-TEST-TMP-DIR above, in a different suite.

## Registry

### BRIDGE-START-BIND

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`)
- Pattern: `Control bridge did not start on port \d+ within \d+s`
- Also matches (same mode, message since reworded): `is still running but did not answer on port \d+`
- Cascade symptom (same mode, not a new entry): `Timed out waiting for app to be ready after \d+s`
- Fix commit: none. The entry previously named an uncommitted working change as the fix, so nothing ever landed and the mode stayed live.
- First seen: 2026-07-06, local 10x loop, run 10 of 10, test 3 (open-database). Runs 1 to 9 passed.
- Recurrences:
  - 2026-07-06, skill loop iteration 7 of 100 (`bun run test:and`), failed on run 10 of 10 with `Control bridge did not start on port N within 20s`.
  - 2026-07-07, skill loop iteration 15 of 100 (`bun run test:and`), failed on run 7 of 10 with the same bridge bind failure.
  - 2026-07-07, skill loop iteration 18 of 100 (`bun run test:and`), failed on run 2 of 10 with the same bridge bind failure.
  - 2026-07-07, skill loop iteration 21 of 100 (`bun run test:and`), failed on run 7 of 10 with the same bridge bind failure.
  - 2026-07-30, five occurrences in one day under `bun run test:everything` (tests 13, 17, 11, 2 and 31; five different ports, five different PIDs, never the same test twice).
  - 2026-07-30, reproduced deliberately 7 times with full evidence: once in `bun run test:and` (test 19, port 42071) and 6 times across 4000 instrumented bridge starts (ports 42691, 35793, 33303, 42329, 40939, 42329). Root cause established, see below. No fix applied.
- Verified: the 2026-07-09 "verified" claim stands only against the old explanation. The real cause below was never addressed, and the mode recurred.
- Root cause: **the probe talks to the Android emulator instead of the bridge, because `localhost` is not `127.0.0.1` as far as curl is concerned.**
  - `ControlBridge.start()` calls `httpServer.listen(0, "0.0.0.0")`, so the bridge listens on **IPv4 only** and the OS assigns a port from the ephemeral range (`net.ipv4.ip_local_port_range` = 32768-60999).
  - Each Android emulator's QEMU process holds several listeners on **`[::1]`** (its console and QMP ports), drawn from that same ephemeral range. With the 5-emulator pool up there are ~25 of them.
  - Binding `0.0.0.0:P` while `[::1]:P` is already held **succeeds**, because they are different address families. Verified directly: every other combination (`127.0.0.1:P` vs `0.0.0.0:P` in either order, `[::]:P` vs `0.0.0.0:P`) is refused with `EADDRINUSE`. So the bridge legitimately ends up sharing a port number with a QEMU console.
  - `wait_for_bridge` probes `http://localhost:$port/ready`. **curl carries its own built-in mapping of the name `localhost` to both `::1` and `127.0.0.1`, and tries IPv6 first**, regardless of `/etc/hosts` or `getaddrinfo`. On this machine `getent hosts localhost` and `getaddrinfo` both return `127.0.0.1` only (there is no `::1 localhost` line), which is why the IPv6 explanation was wrongly dismissed in earlier investigations.
  - So the probe connects to `[::1]:P`, which is the emulator console, not the bridge. The TCP connection **succeeds**, so curl never falls back to IPv4. The console speaks its own protocol, so curl fails at the HTTP layer with exit 1 (`Received HTTP/0.9 when not allowed`) or exit 56 (`Recv failure: Connection reset by peer`).
  - This is deterministic for the whole 40s: all 200 probes hit the console. The bridge is alive, listening and healthy throughout, and answers instantly on `127.0.0.1`.
  - Rate: ~25 QEMU `[::1]` listeners over a 28232-port range is 1 collision per ~1129 bridge starts. The Android suite performs 38 bridge starts per run, so ~3.4% of runs. Observed 6 failures in 4000 starts against 3.5 predicted.
- Evidence (2026-07-30, all 7 reproductions identical in shape):
  - `The control bridge (PID 1634885) is still running but did not answer on port 42071 within 40s (last curl exit 1, 200 probes in 43s).` The probe count is measured, not assumed: it is 200 fast failures, not one hung curl.
  - `curl via localhost: exit 1 -- curl: (1) Received HTTP/0.9 when not allowed`
  - `curl via 127.0.0.1: exit 0` (the bridge answers immediately, in the same second)
  - `curl via [::1]: exit 1 -- curl: (1) Received HTTP/0.9 when not allowed` (identical to the name, which is the point)
  - `lsof -i :42071` shows both owners at once: `bun 1634885 IPv4 TCP *:42071 (LISTEN)` and `qemu-syst 3156721 IPv6 TCP [::1]:42071 (LISTEN)`.
  - The bridge process is `S`/`ep_poll` with 13 open descriptors: idle and healthy, not blocked, not out of descriptors.
  - Confirmed in isolation: with a listener bound only on `[::1]:P` and nothing on `127.0.0.1:P`, `curl http://localhost:P/` reaches it and reports `Received HTTP/0.9 when not allowed`, while `curl http://127.0.0.1:P/` gets connection refused.
- Note on the earlier explanation: the process is not dying and there is no `EADDRINUSE`. The bind always succeeds. The previous entry's three "compounding factors" describe a different failure that a retry-on-EADDRINUSE would not have touched.

### OPEN-DB-LIST-ITEM-NOT-RENDERED

- [x] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), seen via `bun run test:and`
- Pattern: `test-click: element not found data-id="database-list-item-\d+"`
- Cascade symptom (same mode, not a new entry): `Timed out waiting for log pattern: Database opened`
- Fix commit: packages/user-interface/src/lib/test-driver.ts, the `click` command now `await`s a new `waitForElement()` (bounded DOM poll) before clicking, so a click waits for an asynchronously rendered target instead of firing once and silently missing (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-07-06, local 10x loop, run 5 of 10, test 3 (open-database). Runs 1 to 4 passed; within run 5 tests 0 to 2 passed before test 3 failed.
- Recurrences:
  - 2026-07-06, local 10x loop (`bun run test:and`), recurred on run 5 of 10, test 3 (open-database). Runs 1 to 4 passed; within run 5 tests 0 to 2 passed before the click for `database-list-item-0` found no element and `Database opened` never logged.
  - 2026-07-06, local 10x loop (`bun run test:and`), recurred on run 10 of 10, test 21 (import-video). Runs 1 to 9 passed; within run 10 the open-database step opened the dialog, then the click for `database-list-item-0` found no element and `Database opened` never logged.
  - 2026-07-06, local 10x loop (`bun run test:and`), recurred on run 2 of 10, test 4 (import-photos). Run 1 passed; within run 2 tests 0 to 3 passed (including test 3 open-database), then test 4's open-database step opened the dialog, the click for `database-list-item-0` found no element, and `Database opened` never logged.
  - 2026-07-06, skill loop iteration 2 of 100 (`bun run test:and`), recurred on run 6 of 10; click for `database-list-item-0` found no element and `Database opened` never logged.
  - 2026-07-06, skill loop iteration 3 of 100 (`bun run test:and`), recurred on run 5 of 10; same missing-list-item click.
  - 2026-07-06, skill loop iteration 5 of 100 (`bun run test:and`), recurred on run 3 of 10; same missing-list-item click.
  - 2026-07-06, skill loop iteration 6 of 100 (`bun run test:and`), recurred on run 9 of 10; same missing-list-item click.
  - 2026-07-06, skill loop iteration 8 of 100 (`bun run test:and`), recurred on run 3 of 10; same missing-list-item click.
  - 2026-07-06, skill loop iteration 9 of 100 (`bun run test:and`), recurred on run 3 of 10; same missing-list-item click.
  - 2026-07-06, skill loop iteration 10 of 100 (`bun run test:and`), recurred on run 9 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 13 of 100 (`bun run test:and`), recurred on run 4 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 14 of 100 (`bun run test:and`), recurred on run 5 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 16 of 100 (`bun run test:and`), recurred on run 1 of 10; same missing-list-item click (failed on the iteration's first run, but the mode is flaky across the loop, not deterministic).
  - 2026-07-07, skill loop iteration 19 of 100 (`bun run test:and`), recurred on run 4 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 20 of 100 (`bun run test:and`), recurred on run 8 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 23 of 100 (`bun run test:and`), recurred on run 9 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 25 of 100 (`bun run test:and`), recurred on run 9 of 10; same missing-list-item click.
  - 2026-07-07, skill loop iteration 28 of 100 (`bun run test:and`), recurred on run 4 of 10; same missing-list-item click.
  - 2026-07-09, single script run (`bun run test:and`), recurred on run 3 of 10, test 3 (open-database); same missing-list-item click.
- Verified: 2026-07-09, 0 recurrences across 30 consecutive runs (3 full clean 10-run executions) after the fix; no other mode surfaced either.
- Root cause: race in the open-database test. The bridge starts and the app becomes ready fine (not a BRIDGE-START-BIND failure). The "Open database dialog opened" step succeeds, but the dialog's database list is populated asynchronously and the test dispatches its click for `database-list-item-0` before that list item has rendered. `test-click` finds no matching element and silently does nothing, so no database is opened and the `Database opened` log line is never emitted, so the wait-for-log-pattern step times out. The click step needs to wait for the list item to appear (or retry) instead of firing once.
- Evidence: `[INFO] Found: Open database dialog opened (line 5)`, then `[INFO] Waiting for log pattern: Database opened (after line 5)`, then `[WARN] test-click: element not found data-id="database-list-item-0" nth=0`, then `[FAIL] Timed out waiting for log pattern: Database opened` and `FAIL  3-open-database`, ending `1 of 25 tests failed`.

### ASSET-SERVER-THUMB-HTTP-000

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and`), test 1 (load-fixture)
- Pattern: `Asset server returned HTTP 000 for thumbnail [0-9a-f-]+`
- Fix commit: none yet
- First seen: 2026-07-09, single script run (`bun run test:and`), failed on run 5 of 10, test 1 (load-fixture). Runs 1 to 4 passed (each served the thumbnail fine).
- Recurrences: none
- Root cause: not yet fully investigated; leading hypothesis is a logcat-staleness / asset-server-startup race in the load-fixture test's host-side thumbnail probe. The test reads the asset server port with `adb logcat -d | grep 'Asset server task listening on http://127.0.0.1:<port>' | tail -1`, then curls it. `adb logcat -d` dumps the whole buffer, which accumulates listening lines from every prior run's app instance in the same suite invocation. If the current run's asset server has not yet logged its listening line when the test greps, `tail -1` yields a previous run's now-dead port, and the curl to it fails to connect, which curl reports as HTTP 000 (the app itself is fine: the DB opened and 50 assets loaded). Needs confirmation, and likely a fix that scopes the port read to the current run (clear logcat at test start, or match the current app instance) or waits for the current run's listening line before probing.
- Evidence: runs 1 to 4 each logged `[PASS] Asset server served a JPEG thumbnail over localhost:<port>`; run 5 logged `[FAIL] Asset server returned HTTP 000 for thumbnail 63e9c637-9164-6376-13e9-ef3200000000` then `FAIL  1-load-fixture`, ending `1 of 25 tests failed`. The snapshotted app.log for that run shows `Load assets task completed: 50 assets loaded` and `Gallery loaded: 50 assets`, so only the host-side asset-server probe failed, not the app.

### CONFIG-READ-TASK-LOST

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and`), any test whose first step opens a database. Seen in test 3 (open-database) and test 4 (import-photos).
- Pattern: `Timed out waiting for log pattern: Open database dialog opened`
- Distinguishing evidence (this mode, not OPEN-DB-LIST-ITEM-NOT-RENDERED, which shares those tests): the app log ends at `Open database dialog opening` with no `Open database dialog opened` after it. In that other mode the dialog opens and the failure is a later click on `database-list-item-0`.
- Fix commit: none for the root cause. A bounded wait and a read retry landed in packages/mobile-frontend/src/lib/mobile-databases-config-file.ts and packages/mobile-frontend/src/lib/await-with-timeout.ts (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 132 of a 500-run `bun run test:everything -- --force` loop, in test 3.
- Recurrences:
  - 2026-08-02, run 175 of the same loop, in test 4, 42 runs after the previous reset. This occurrence carried the diagnostics added after run 132 and is what identified the mode.
- Clean runs since the change: 35 passing runs (176 to 210), no recurrence. The box above stays unticked deliberately, for two reasons. The root cause is still unidentified, so nothing here is a fix. And a bounded wait plus a retry would let a recurrence pass green, which would make the ledger a useless witness on its own, so the run logs were searched directly instead: no `read-databases-config did not complete` timeout and no retry warning appears in any run from 176 onward. The lost-task condition has therefore not recurred at all in those 35 runs, rather than recurring and being papered over. At a measured rate near 0.6% per database-opening test, 35 runs is far too few to mean much either way.
- Rate: 1 in 152 retained runs of test 3 and 1 in 199 of test 4, so roughly 0.6% of the runs of a test that opens a database.
- Root cause: **partially established.** The dialog opens and `platform.getDatabases()` never settles: it neither resolves nor rejects, so the modal's `loaded` flag stays false, the gated "opened" line never fires, and the test waits out its full 120s. The read reaches the embedded JS engine through `runConfigTask`, which does `queue.addTask(...)` then `await queue.awaitTask(taskId)`, and `awaitTask` has no deadline. What is established: the task was handed over and its completion never came back. What is not established: which link lost it. Three candidates carry silent-drop paths, all of which leave the JS side waiting for ever because dispatch is fire-and-forget and the plugin call resolves regardless: `EnginePool.addTask` returns without emitting a completion when the pool is shutting down or when the task's source is already cancelled (apps/android-frontend/android/app/src/main/java/au/com/codecapers/photosphere/jsengine/EnginePool.java, both `return` statements in `addTask`), and an engine that never reports a terminal outcome leaves its task in `runningByTaskId` for ever. Neither `EnginePool` drop obviously fits this occurrence, because each config task is given a freshly generated unique source and the app had just started, so the third candidate is the open one. Both failures were the first config read after app start, which is the strongest remaining clue.
- What was changed: `runConfigTask` now waits at most `CONFIG_TASK_TIMEOUT_MS` (20s) and throws an error naming the task type, so a lost task fails loudly and immediately instead of hanging invisibly; the modal's existing `catch` then logs it. `read()` additionally retries once, which is safe because a read run twice gives the same answer, while `write()` deliberately does not retry. **The retry is a mitigation for a task lost on the way to the engine, not a cure for whatever loses it.** Separately, `dispatchPlatformEvent` now warns rather than silently dropping a platform event with no subscriber, and the dialog logs `Open database dialog opening` before it starts loading; those diagnostics are what made this occurrence readable.
- Evidence: run 175, `apps/smoke-tests/tests/4-import-photos/tmp/run-2723526/app.log`, ending exactly at `[INFO] Open database dialog opening` with no error line and no `Platform event dropped` warning, which eliminates the dropped-event half of the earlier OPEN-DB-DIALOG-NEVER-OPENED entry. 198 other retained runs of that test all continue `Open database dialog opened` on the next line. A rejection would have logged `Failed to load databases:` through the modal's catch, and none was logged, so the promise never settled at all.

### READY-RELAUNCH-STALE-PORT

- [x] Fixed and verified (10x clean). 96 passing runs since the fix landed at run 112 (runs 113 to 210 of the 2026-08-02 loop). The pattern `curl failed (exit N) posting to` appears in none of the 99 retained logs from that range, failed runs included.
- Suite: Electron smoke tests (`bun run test:electron`), tests 7 (share-secret) and 8 (share-database)
- Pattern: `curl failed \(exit \d+\) posting to \w+`
- Cascade symptom (same mode, not a new entry): `Timed out waiting for log pattern: .+` immediately following the curl failure
- Fix commit: apps/desktop/smoke-tests/lib/common.sh, `wait_for_ready` takes the caller's port variable name and writes the live instance's port back into it; tests 7 and 8 pass their variable names (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-07-31, full `bun run smoke`, test 8 (share-database). An immediate standalone re-run passed. Diagnosed in the `diagnose-8-share-flake` worktree, where the fix was written and verified, but that work never reached the `mobile` branch.
- Recurrences:
  - 2026-08-02, run 112 of a 500-run `bun run test:everything -- --force` loop on the `mobile` branch, 13 runs after the previous reset. The branch did not carry the fix, so the mode was still live here: launch attempt 1 timed out after 120s, the relaunch succeeded on a new port, and the next command failed with `curl failed (exit 56) posting to navigate`, then `Timed out waiting for log pattern: Databases page loaded`.
- Root cause: `wait_for_ready`'s relaunch path moves the app to a new port and returns success without telling the caller, so a test that copied the port before calling it goes on driving the instance that was just killed. `wait_for_ready` recovers a wedged launch by calling `start_app` again, which overwrites the `APP_PORT` global with the relaunched instance's OS-assigned port, and its readiness poll reads that global rather than its `<port>` argument, so it reports ready against the new instance. Tests 7 and 8 are the only tests that copy `APP_PORT` into their own variable (they must, because they run two instances and the second `start_app` overwrites the global), and they copy it before calling `wait_for_ready`. After a relaunch every later `send_command` in the test posts to the dead port.
- Evidence: the run 112 transcript above, and the original investigation in the `diagnose-8-share-flake` worktree: a deterministic reproduction that forces the relaunch path passed 100 of 100 after the fix and failed every run before it. The shell unit test `apps/desktop/smoke-tests/lib/common.test.sh` (run by `bun run test:desktop-smoke-lib`) asserts the caller's port variable holds the live instance's port after a relaunch; removing the write-back fails 2 of its assertions, confirmed on this branch.

### SHARE-DB-REVIEW-STEP-NEVER-REACHED

- [ ] Fixed and verified (10x clean)
- Suite: Electron smoke tests (`bun run test:electron`), test 8 (share-database). The same root cause also fails the CLI LAN-share suite (`bun run test:cli:lan-share`).
- Pattern: `Timed out waiting for log pattern: Database review step`
- Also matches (same root cause, CLI LAN-share suite): `No-secrets DB share: sender did not report success`, accompanied by `Pairing code rejected by other device.`
- Also matches (same root cause, CLI LAN-share suite, its rogue-process test): `Rogue test: expected 403 but got \d+`
- Fix commit: the discovery announcement now carries the pairing code's hash and a sender ignores any receiver that is not its own (packages/lan-share-network/src/lib/lan-share-receiver.ts and lan-share-sender.ts); both give-up paths now log (share-database-dialog.tsx, receive-database-dialog.tsx) (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-01, run 3 of a 100x `RUNS=100 scripts/check-flaky-tests.sh bun run test:electron -- 8` loop.
- Recurrences:
  - 2026-08-02, run 2 of a `bun run find-flakey-tests` session, exactly the recorded signature. This is what forced the fix: the mode had been left unfixed on the grounds that it had not yet been seen in this loop.
- Fix: **the announcement now carries session identity.** It changed from `PSIE_RECV:{port}:{fingerprint}` to `PSIE_RECV:{port}:{codeHash}:{fingerprint}`, and the sender ignores any announcement whose hash is not the one it is looking for. The hash goes before the fingerprint because a fingerprint can contain colons. This publishes nothing new: it is the same unsalted `sha256(code)` any caller can already fetch from the receiver at `GET /pairing-code-hash`, unauthenticated, on a receiver that is broadcasting its port to the whole subnet. That a 4-digit code is brute-forcible from its hash is a pre-existing property of the pairing design and is not made worse here.
- Verified: the deterministic reproduction from the investigation now reports `discovery did NOT cross sessions` where it used to cross. Electron test 8 passed 3 times in a row with the noise generator advertising a foreign receiver throughout, against 3 of 3 failures before the fix. The CLI LAN-share suite passes 18 of 18. A unit test covers the new behaviour and was watched failing against the old predicate.
- Trade-off this fix introduces, deliberately: a sender given the wrong pairing code used to fail fast, because it latched onto the receiver and was refused. It now holds out for its own receiver and reports only when the 60s discovery window closes. `LanShareSender.sawMismatchedReceiver` records that a receiver with a different code was heard, so the CLI says "a device was found but it is using a different code" rather than "no device found". `test_wrong_pairing_code` in the CLI suite now needs 90s instead of 20s, taking that suite from 31s to 71s.
- Known gap, not fixed: the same distinction is not yet plumbed to the desktop and mobile UI. `platform.waitForReceiver` returns only the endpoint, so a mistyped code in the app shows "No receiver found within 60 seconds." where it used to show "Pairing code rejected by receiver." Closing it means carrying `sawMismatchedReceiver` through `IFindReceiverTaskResult` (packages/api/src/lan-share/index.ts), `findReceiverHandler` (packages/node-api/src/lib/lan-share.worker.ts), `use-lan-share-tasks.ts` and the platform context, then reading it in share-database-dialog.tsx and share-secret-dialog.tsx.
- Root cause: LAN-share discovery carries no session identity, so a sender commits to the first receiver it hears and gives up for good if the pairing code does not match. Test 8 opens that discovery window about 5 seconds before its own receiver starts advertising, so any unrelated share announcing during those seconds takes the sender's only pick. The broadcast is `PSIE_RECV:{port}:{fingerprint}` with nothing identifying the session; the sender accepts the first message with that prefix and closes its socket; a code mismatch makes `send()` return false with no retry and no re-discovery. Both give-up paths only set on-screen text and log nothing, which is why the test sees no error, just the downstream 120s log-pattern timeout. Competing announcements come from another worktree's Electron suite, from the lan-share unit tests (same fixed port 54321, scheduled in a parallel lane alongside `test:electron` by `scripts/test-everything-parallel.sh`), or from any machine on the LAN, since the announcement goes to `255.255.255.255`.
- Evidence: `docs/investigations/lan-share-discovery-crosstalk/` and `docs/investigations/electron-8-share-database-flake/` in the `diagnose-8-share-flake` worktree hold the measurements, the reproduction scripts and the suggested fixes.

### IMAGEMAGICK-RESIZE-PRODUCED-NO-FILE

- [ ] Fixed and verified (10x clean). Not fixed: the reason ImageMagick fails is not established. What changed is that the failure is now loud and carries the tool's own output.
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), any test whose fixture is built by the CLI. Seen in test 32 (encrypted-database). The CLI itself is affected, so this is not only a test problem.
- Pattern: `Resize failed, expect to create .*temp_resize_`
- Cascade symptom (same mode, not a new entry): `Timed out waiting for log pattern: Gallery items rendered`, with `Load assets task completed: 0 assets loaded` in the app log, because the fixture ended up with no assets.
- Fix commit: none for the cause. `apps/cli/src/cmd/add.ts` now exits non-zero when any file failed to import, and `packages/node-utils/src/lib/exec.ts` now puts the tool's stdout and stderr into the thrown error (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 6 of a 500-run `bun run find-flakey-tests` session, 5 runs after the previous reset.
- Recurrences: none yet.
- Root cause: **not established.** ImageMagick's `convert ... -resize 1333x1000 -quality 95 jpeg:<temp>` returned without producing its output file, and the import of that one file failed. Why it produced no file is unknown, because the only place the tool's own stderr was written was the CLI's log file, which no longer existed by the time the failure was investigated.
- Two separate defects were found while diagnosing it, both now fixed, neither of which is the cause:
  - **`psi add` exited 0 when files failed to import.** It printed `Files failed: 1` and a warning, then `await exit(0)`. Test 32 checks the exit status of its CLI calls and still walked straight past the failure, seeded an assetless database, and failed 133 seconds later complaining about an empty gallery. For a person importing photos this is worse than a test problem: the command reported success while photos were missing. Verified both ways after the change, a file that is not a real image now exits 1, and a good import still exits 0 with `Files added: 1`.
  - **`execLogged` threw away the tool's output.** The thrown error carried only the validation reason, and the stdout and stderr went to a log file. That is why this occurrence cannot be explained. They are now part of the error message, so the next occurrence says what ImageMagick actually reported.
- What to look for next time: the error will now read `Resize failed, expect to create ...` followed by `magick stdout:` and `magick stderr:`. Resource exhaustion under the parallel suite and a missing temp directory both look plausible from here, and neither is established, so the tool's own words are what will settle it.
- Evidence: session `tmp/find-flakey-tests/20260802-153948`, run 6. The snapshot under `suite-logs-run-0006/` holds the CLI output showing the failed convert, `Files failed: 1`, and the test continuing regardless. After the change: `bun run test:cli` passes all 65 tests and the full unit suite passes, so the new exit code breaks nothing that relied on the old one.

### CLI-SUITE-WIPED-SHARED-TEMP-DIR

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), any test whose fixture is built by the CLI. Seen in test 18 (move-file). The cause is in the CLI smoke suite, which runs in a parallel lane alongside it.
- Pattern: `ENOENT: no such file or directory, open '.*/photosphere/logs/psi-.*\.log'`
- Cascade symptom (same mode): `Creating the database at .* failed`, and before the fixture builder checked its exit status, a downstream `Timed out waiting for log pattern: Gallery items rendered`.
- Fix commit: `export TEST_TMP_DIR` in apps/cli/smoke-tests.sh (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 13 of a `bun run find-flakey-tests` session, 12 runs after the previous reset.
- Recurrences:
  - 2026-08-04, `bun run test:everything` on the `parallel-test-interference` branch, failing Electron tests 18 (move-file) and 19 (download-single-asset), both at 0s, both with `ENOENT: no such file or directory, open '/tmp/photosphere/logs/psi-2026-08-04T07-38-44-errors.log'` while their fixture builder shelled out to the CLI. The fix had never reached this branch: `apps/cli/smoke-tests.sh:57` still assigned `TEST_TMP_DIR` without `export`. It has now been applied here, with a comment saying why the `export` matters, so a later tidy-up cannot drop it again as a redundant keyword.
- Verified on this branch, both directions, by placing a marker file at `/tmp/photosphere/logs/` and running `hash-cache clear`: with `TEST_TMP_DIR` in the environment the CLI reports "Local hash cache not found or already empty" and the marker survives; without it the CLI reports `✓ Cleared hash cache at: /tmp/photosphere` and the marker is gone.
- Root cause: **a shell variable that was set but never exported, so a child process never saw it.** `apps/cli/smoke-tests.sh` assigned `TEST_TMP_DIR` without exporting it. The CLI is a child process, so `getProcessTmpDir()` fell back to `os.tmpdir()` and every CLI process on the machine shared `/tmp/photosphere`. That suite runs `hash-cache clear` four times, and `clearCacheCommand` does `fs.rmSync(getProcessTmpDir()/photosphere, { recursive: true })`, deleting the shared tree outright, `logs/` included. Meanwhile the mobile suite, in a parallel lane under `test:everything`, was building a fixture: `FileLogger.create` calls `ensureDirSync(logsDir)` and then shells out three times for ImageMagick, ffmpeg and ffprobe versions before writing the header, which is a wide enough window to lose the directory underneath it.
- Note the code already knew about this: the comment above `getProcessTmpDir()` in apps/cli/src/lib/file-logger.ts describes this exact failure and names `TEST_TMP_DIR` as the mechanism that prevents it. The mechanism was correct and simply was not switched on, because `export` was missing one line away from it.
- Evidence: run 13's snapshot, `apps/smoke-tests/tests/18-move-file/tmp/run-1090242/test-run.log`, showing `ENOENT ... open '/tmp/photosphere/logs/psi-2026-08-02T08-48-28.log'` inside `writeLogHeader`. Proved directly rather than inferred: run `hash-cache clear` with the variable unexported and it reports `Cleared hash cache at: /tmp/photosphere`; export it and it reports the cache is not found there, staying inside its own directory. After the fix a marker file placed in `/tmp/photosphere/logs` survived a full `bun run test:cli` run, which passed 65 of 65, where before that run would have deleted it four times.
- Also set-without-export, not fixed because neither runs a command that deletes the shared tree: apps/cli/smoke-tests-encrypted.sh and apps/cli/smoke-tests-lan-share.sh. They share `/tmp/photosphere` rather than wiping it, which is harmless now the wipe is gone.

### S3-EMULATOR-SEEDED-BEFORE-READY

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), test 40 (s3-database). Any suite that starts the local S3 emulator can hit it.
- Pattern: `Could not start the local S3 emulator`
- Distinguishing evidence (this mode, not a genuinely broken emulator): the seed's error is `XMinioServerNotInitialized`, and the server log printed alongside it still reads `Formatting 1st pool, 1 set(s), 1 drives per set`. The test fails in about 2 seconds, so it never reaches its own body.
- Fix commit: `start_emulator` in scripts/s3-emulator.sh now retries the bucket seed while MinIO reports it is not initialised, up to `SEED_ATTEMPTS` (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 1 of a 500-run `bun run find-flakey-tests` session.
- Recurrences: none yet.
- Root cause: **answering the health check does not mean MinIO can serve.** `wait_for_health` polls `/minio/health/live` and returns as soon as it gets a 200, and the seed's very next S3 request came back `XMinioServerNotInitialized` while the server was still formatting its storage pool. Under the full parallel suite the formatting takes long enough for that window to be reachable. A tempting fix is to poll `/minio/health/ready` instead, and that is wrong: measured directly against the pinned MinIO (RELEASE.2025-09-07T16-13-09Z) on a fresh data directory, both endpoints turned 200 at the same moment, 189ms after launch, so the two carry the same information here. The only reliable readiness signal is a real S3 operation succeeding, and the seed is exactly such an operation, so the seed now does the waiting rather than a proxy for it.
- Why the retry is narrow: only `XMinioServerNotInitialized` is retried. Any other failure, a wrong credential for instance, is reported immediately with the CLI's output rather than being retried ten times until the cause is buried under a timeout.
- Evidence: session `tmp/find-flakey-tests/20260802-153213`, run 1, test 40 failing in 2s with the seed error and the formatting line. The endpoint timing was measured directly by launching the pinned binary on a fresh data directory and polling both endpoints every 50ms from launch. Both branches of the fix were verified by patching the seed script to fail on demand: two forced `XMinioServerNotInitialized` failures were retried and the third succeeded, and a forced `AccessDenied` failed on the first attempt without retrying. Test 40 passes with everything restored.

### FIXTURE-DATABASE-BUILT-EMPTY

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), any of the 13 tests that call `create_database`. Seen in tests 19 (download-single-asset) and 20 (download-multiple-assets), both in the same run.
- Pattern: `Timed out waiting for log pattern: Gallery items rendered`
- Distinguishing evidence (this mode, not a rendering problem): the app log shows the database opening normally and `Load assets task completed: 0 assets loaded`, and the seed line in the test log pushes far less than a working run. Test 19 pushes 15 files and about 34KB when it works and pushed 5 files and 1530 bytes when it failed; test 20 pushes 20 files and about 68KB when it works. Five files is the bare structure `psi init` creates, so the database was made and the image was never imported.
- Fix commit: `create_database` in apps/smoke-tests/lib/common.sh now checks the exit status of every CLI call and prints the CLI's output when one fails (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 1 of a 500-run `bun run find-flakey-tests` session, tests 19 and 20 failing together.
- Recurrences: none yet.
- Root cause: **the fixture builder threw its errors away.** `create_database` ran `bun run start -- init` and then `bun run start -- add` for each image, each with `>/dev/null 2>&1` and no check of the exit status. When an `add` failed, the function returned success anyway, the test seeded an assetless database onto the device, opened it, and got an empty gallery. The failure then surfaced 132 seconds later as a timeout waiting for `Gallery items rendered`, which describes an empty gallery and says nothing about the import that did not happen. Both failing tests are ones that import a fixture, and both failed in the same run, which is what a shared host-side helper failing under load looks like rather than two independent device problems.
- What this fix does and does not cover: the CLI does exit non-zero when an import fails, confirmed directly by pointing `create_database` at a path that does not exist, so a failing call is now caught and named at the point it happens. What is not established is whether the original occurrence was a non-zero exit, because the status and the output were both discarded and cannot be recovered. If the mode ever recurs with the CLI exiting zero while importing nothing, that is a different and deeper bug in the CLI, and this check will not catch it: the next step then would be to assert the database actually holds the images after the import.
- Evidence: session `tmp/find-flakey-tests/20260802-152250`, run 1. `report.txt` names both tests. The snapshot under `suite-logs-run-0001/` preserved both tests' logs, which mattered here: re-running the two tests to tell a flake from a regression overwrote the originals, and the snapshot is the only reason the 5-files-versus-15-files comparison was still possible. Both tests passed on their own immediately afterwards, so this is intermittent and not a regression.

### DESKTOP-THUMBNAIL-NETWORK-ERROR

- [ ] Fixed and verified (10x clean). Not fixed: the cause is not established, and the change made here only makes the next occurrence readable.
- Suite: Electron smoke tests (`bun run test:electron`), test 20 (download-multiple-assets). Any test that renders a gallery and asserts on a clean app.log could show it.
- Pattern: `Failed to load asset: thumb:[0-9a-f-]+`
- Distinguishing evidence: the test's functional steps all succeed. In the observed case the log ends `Download to folder completed: 2 assets downloaded` and the only reason the test failed is the `Errors found in app.log` assertion.
- Fix commit: none. `describeHttpError` in packages/user-interface/src/lib/describe-http-error.ts is now appended to the three asset-load failure messages in gallery-image.tsx and full-image.tsx, so the client's error code and any HTTP status are recorded (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 247 of a 500-run `bun run test:everything -- --force` loop, 15 runs after the previous reset.
- Recurrences:
  - 2026-08-02, run 3 of a `bun run find-flakey-tests` session, this time in the Electron suite's own test 20 rather than the mobile one. The diagnostics added after the first occurrence worked: the log now reads `Failed to load asset: thumb:<id> (code=ERR_NETWORK, status=0)`, which is what identified the retry's predicate as wrong. As before the test's functional steps all passed, both files downloaded and matched the originals byte for byte, and only the `Errors found in app.log` assertion failed.
- Root cause: **not established.** What is known: the asset server bound and logged `Asset server running on http://localhost:37371` at line 6 of app.log, long before the window was created at line 12 and the gallery rendered at line 30. Both of the database's two assets then failed their thumbnail fetch, and the app kept working: the long press, the sidebar, the checkbox and the download all succeeded afterwards, and both assets downloaded. So the server was up, the app was healthy, and two requests to a live loopback port got no response. Each failure is logged twice because the renderer's log lines are all duplicated in this app.log, so it is two failed loads and not four. The desktop path does not use `useAssetServer`: `apps/desktop-frontend/src/app.tsx` reads `restApiUrl` from a query parameter and throws when it is missing, so an unset url is ruled out. What is not known: whether the connection was refused, reset, or resolved to an address nothing was listening on, because the client reports every one of those as the same bare `Network Error`.
- Why it was not diagnosable: the catch block logged `Failed to load asset: thumb:<id>` and the error's message, and an HTTP client's message for any no-response failure is the constant string `Network Error`. No code, no status, nothing to separate ECONNREFUSED from ECONNRESET from a bad address. The change records the code and status. **It does not stop the mode**: the run still fails and the green count still resets. It makes the next occurrence say which failure it was.
- Retry (added on the human's instruction, after the entry above was first written): every GET through `axiosApi` retries a transient failure, up to `HTTP_REQUEST_ATTEMPTS` (3) attempts `HTTP_RETRY_DELAY_MS` (250ms) apart, logging a warning on each retry (packages/user-interface/src/lib/retry-http-request.ts).
- **The first version of that retry never fired for this mode, and the claim that it would stop the mode was wrong.** `isTransientHttpError` asked only whether the error carried a response object. This failure carries one, with `status: 0`, which is not an answer from the server but what the client fills in when the request failed at the network layer. The predicate read that as a considered answer and did not retry. Proved by the recurrence below: four `Failed to load asset` errors and zero retry warnings in the same app log. The predicate now also treats a response with status 0 as transient. It still does not explain why the request failed, and the retry warning is the thing to look for in a future log. POST is deliberately not retried: the database ops it carries include `push` and `pull`, so re-sending a request whose response was merely lost would apply the op twice.
- Root cause, established on the second recurrence: **the renderer was told to reach an IPv4-only server by a name that can resolve to IPv6 first.** `createAssetServer` binds `127.0.0.1` only (packages/rest-api/src/lib/asset-server.ts), while `apps/desktop/src/main.ts` handed the renderer `http://localhost:<port>`. Nothing normally listens on `[::1]:<port>`, so the connection falls back to IPv4 and the name appears to work. But the server takes an OS-assigned port from the ephemeral range (32768-60999) and each Android emulator holds console ports on `[::1]` from that same range: 25 of them were confirmed live with the pool up. When the two collide the connection to `::1` succeeds, so there is no fallback, and every asset request reaches an emulator console instead of the asset server. This is the same mechanism as BRIDGE-START-BIND, which was reproduced 7 times in this repository and fixed the same way.
- Fix: the URL is now the literal `http://127.0.0.1:<port>` in apps/desktop/src/main.ts, in packages/user-interface/src/lib/use-asset-server.ts and in the asset server's own log line, and `apps/desktop-frontend/index.html` now allows `http://127.0.0.1:*` and `ws://127.0.0.1:*` in its content-security policy.
- **What is proved and what is not.** Proved: the server binds IPv4 only; the URL used the name; 25 QEMU listeners hold `[::1]` ports in the same ephemeral range; the failure persisted across all three retry attempts within ~500ms, so it was not transient. Not proved: that Chromium chose `::1` in either occurrence. The ports involved (37371, 35617) are long gone and cannot be checked after the fact, and a probe written to demonstrate the hazard showed Bun's `fetch` prefers IPv4, so it does not stand in for Chromium. The fix is justified regardless, because binding one address family and connecting by a name that can resolve to another is wrong on its own terms, but the causal link is inference and the next recurrence is what would disprove it.
- **A trap found while fixing it, worth knowing:** switching to the literal without updating the content-security policy turned a roughly 0.4% intermittent failure into 5 of 25 Electron tests failing every single run, with a byte-identical signature: `code=ERR_NETWORK, status=0`, persistent, retries not helping. A CSP block and a connection to the wrong process are indistinguishable in the log. That is worth remembering before reading this signature as a network problem again.
- Not done deliberately: the test's `no errors in app.log` assertion was left exactly as it is. The app really did log an error, and loosening the assertion would hide the mode rather than fix it.
- Evidence: `apps/desktop/smoke-tests/20-download-multiple-assets/tmp/app.log` from run 247, lines 6, 30 to 39 and 44. Test 20 failures across the loop: runs 98 and 247, of which only 247 contains `Failed to load asset`; run 98 was the OOM event. The new helper was watched failing on all four of its unit tests against a deliberately broken version before being accepted.

### EMULATOR-VANISHED-MID-RUN

- [ ] Fixed and verified (10x clean). Not fixed: the cause of the emulator's death is not established, and the change made here only names the failure.
- Suite: mobile smoke tests (`bun run test:and`), any test, whichever one happens to hold the device that dies. Seen in test 39 (secret-in-keychain).
- Pattern: `The emulator this test is bound to \(emulator-\d+\) is no longer attached` (after the change below). Before it, the mode presented as `Timed out waiting for app to be ready after \d+s` followed by `App failed to become ready after 2 launch attempts`, which names the symptom and not the cause.
- Shape that identifies this mode: one test fails while the rest of the suite passes on other devices, the test's own log shows an earlier phase completing normally, and `adb: device offline` or `adb: device 'emulator-NNNN' not found` appears between the last good step and the timeout. `adb devices` afterwards lists one fewer device than the pool size.
- Fix commit: none for the cause. `android_require_device_present` in apps/smoke-tests/lib/android.sh, called from `wait_for_ready` in apps/smoke-tests/lib/common.sh, now stops with a named error instead of retrying against a serial that is gone (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-02, run 231 of a 500-run `bun run test:everything -- --force` loop, 55 runs after the previous reset. Test 39 failed after 256s.
- Recurrences: none yet.
- Root cause: **not established.** What is known: emulator-5560 (host pid 2025871) stopped at 12:57:57, about two minutes into run 231. Its own log, `/tmp/psphere-emulator-pool-3.log`, ends mid-stream on routine `Created VkInstance` / `Destroyed VkInstance` lines with no error, no warning and no shutdown message, which is what a process killed outright looks like rather than one exiting. What has been ruled out: the kernel OOM killer did not act (the last `oom-kill` was at 09:33:48, more than three hours earlier, and none appears in the run's window); `systemd-oomd` is running but recorded nothing; no `segfault`, `general protection` or `traps:` line for any qemu process appears in the kernel log for the whole day; and nothing in the test suite kills emulators, the only `emu kill` calls being in `cmd_pool_down` and `stop_emulator` in apps/android-frontend/scripts/emulator.sh, neither of which a test run invokes and both of which echo a "Stopping..." line that is absent. Host memory was not tight at the time: 34GB available, 14GB of 65GB swap in use, and the four surviving emulators holding 20GB between them. So the emulator was killed or died without leaving a trace in any log searched, and the mechanism is unknown.
- Why the test took 256s: `wait_for_ready` retries by relaunching the app, which cannot help when the device itself is gone, so it spent two full 120s timeouts on a serial adb had already reported as `not found`. That is what the change addresses. **It does not stop the mode**: the run still fails and the green count still resets. It converts a 256s misleading failure into an immediate accurate one, so the next occurrence is diagnosable and cheap.
- What would settle the cause: the pool launches each emulator with `>"/tmp/psphere-emulator-$log_suffix.log" 2>&1 </dev/null &` and never records how the process ended, so a death by signal is indistinguishable from any other. Capturing the wait status of each pool emulator would name the signal next time. That change is in apps/android-frontend/scripts/emulator.sh, which this repository's rules put under the human's control, so it has not been made.
- Evidence: run 231 log (`FAIL 39-secret-in-keychain 256s`, `1 of 37 tests failed`); the test's own log showing `Secret added` and `No plaintext secret copy in localStorage after add (good)` completing, then `adb: error: closed`, `adb: device offline`, then three `adb: device 'emulator-5560' not found` lines across the two attempts; `/tmp/psphere-emulator-pool-3.log` last written 12:57:57 against run 231's window of 12:56:03 to 13:02:13. The new check was exercised against live adb with the genuinely-absent emulator-5560, a nonexistent serial, an attached serial and an unset serial, giving 1, 1, 0 and 0 respectively.

### EMULATOR-POOL-OOM-KILLED

- [ ] Fixed and verified (10x clean)
- Suite: mobile smoke tests (`bun run test:and`), all tests at once rather than any one of them
- Pattern: `adb: device offline`
- Shape that identifies this mode: nearly every test in the suite fails at 0s (36 of 37 seen), not one test in isolation, and `adb devices` afterwards lists nothing at all.
- Fix commit: none in this repository. This is a host capacity limit, not a defect in a test. The loop harness now refuses to start a run below a memory floor.
- First seen: 2026-08-02, run 98 of a 500-run `bun run test:everything -- --force` loop, 27 runs after the previous reset.
- Recurrences: 1. Run 208 of the same loop, 32 runs after the previous reset, killed at 09:33:48 with the pool holding 29GB across five emulators. The run was discarded rather than counted as a failure, because no test reported anything: the kill took the run's own processes with it. The memory floor did not catch it, because the floor is sampled before a run starts and run 208 started with 25GB available; the suite itself then took better than 20GB while the pool was already at its grown size. The loop harness now also refuses to start when the pool has grown past a ceiling, which is the signal that actually predicts the kill.
- Root cause: **the host ran out of memory and the kernel OOM killer took the emulator pool with it.** The Android emulators are long-lived across runs and grow steadily: measured at 3.2GB resident each on a freshly started pool (15.8GB across the five) against 5.0GB to 6.6GB each (~28GB) after about 28 consecutive suite runs, roughly 0.4GB per run across the pool. On a 62GB machine that is survivable for a few dozen runs and then is not. Nothing in the suite is at fault, and no single test can be blamed: once the emulators die, every test that needs a device fails instantly with `adb: device offline`. This only shows up when the suite is run back to back many times, which is why it had not been seen before.
- Evidence: `journalctl -k` records `oom-kill` and `Out of memory: Killed process` at 02:45:49, inside run 98's window of 02:43:06 to 02:47:52. The kernel's task dump at that moment lists the five pool `qemu-system-x86` processes at 1313246, 1306580, 1724076, 1488042 and 1416703 pages resident (5.0GB to 6.6GB each). Afterwards `adb devices` listed no devices, and the run's first failing test logged `Could not clear the app's stored data on emulator-5554: adb: device offline`. Restarting the pool with `bun run emu:and:pool` brought all five back on the bridge at 3.2GB each.
- Mitigation: recycle the emulator pool periodically when looping the suite. A pool that has been up for tens of runs is holding several times its fresh footprint.

### LAN-BRIDGE-PROBE-SINGLE-SAMPLE

- [x] Fixed and verified (10x clean). 136 passing runs since the fix landed at run 69 (runs 70 to 210 of the 2026-08-02 loop). The pattern appears in none of the 142 retained logs from that range, failed runs included. Note what this does and does not show: the fix makes a single missed sample harmless, and the mechanism of that miss is still unestablished, so this only demonstrates that the mode no longer fails a run.
- Suite: mobile smoke tests (`bun run test:and`), tests 26 (receive-database) and 27 (receive-secret), the only two that call `require_lan_bridge`
- Pattern: `The emulator is not on the host LAN bridge`
- Fix commit: apps/smoke-tests/lib/common.sh, `require_lan_bridge` now samples wlan0 up to `LAN_BRIDGE_PROBE_ATTEMPTS` times and reports adb's actual output when it fails (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-01, run 69 of a 500-run `bun run test:everything -- --force` loop, 19 runs after the previous reset. Test 27 failed in 0s.
- Recurrences: 1 occurrence in 176 retained run directories for the two bridge-requiring tests (~0.6%).
- Root cause: **the probe took a single sample of a static property and threw the evidence away.** `require_lan_bridge` ran `adb shell ip addr show wlan0 2>/dev/null | grep -q 'inet 192\.168\.55\.'` once and, on any miss, exited 1 telling the developer to run `emu:and:up`. Because stderr went to `/dev/null`, an adb hiccup and a genuinely missing bridge produced byte-identical output, so the log could not tell them apart. The bridge was demonstrably up: the same run's own pre-flight check recorded `emulator-5554: on the lan bridge (wlan0 = 192.168.55.125/24)`, test 26 passed on another emulator, and tests 32 and 40 then ran on emulator-5554 in the same run without trouble. The exact reason one sample missed is not established (2600 probes across all five emulators under suite load produced 0 misses, so a plain adb failure under load is not it). The fix does not depend on knowing: the bridge does not come and go during a run, so several samples decide it instead of one, and adb's real output is now printed when it fails, which is what a future occurrence needs.
- Evidence: run 69, `27-receive-secret` failed in 0s with the log containing only the test header and the two failure lines. Run 69 line 37: `emulator-5554: on the lan bridge (wlan0 = 192.168.55.125/24)`. Fix verified on both paths: with a serial that cannot answer it still fails hard after 5 samples in 5s and now reports `adb: device 'emulator-9999' not found`; with the probe pointed at an address range that does not exist it fails and prints the real wlan0 output; against a real emulator it succeeds in 0s. Tests 26 and 27 both pass with the fix.

### CREATE-DB-CONFIRM-CLICKED-WHILE-DISABLED

- [x] Fixed and verified (10x clean). 155 passing runs since the fix landed at run 49 (runs 50 to 210 of the 2026-08-02 loop). The pattern appears in none of the 162 retained logs from that range, failed runs included.
- Suite: mobile smoke tests (`bun run test:and` / `bun run test:ios`), test 31 (create-database-no-collision)
- Pattern: `Timed out waiting for log pattern: Database created`
- Distinguishing evidence (this mode, not another timeout of the same wording): the app log's last line is `test-click: clicking element data-id="create-database-confirm"` with no `Database entry added` after it.
- Fix commit: apps/smoke-tests/tests/31-create-database-no-collision/test.sh now waits for `database-path-input` to hold the browsed path before clicking Create, and packages/user-interface/src/lib/test-driver.ts now warns when a click lands on a disabled control (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-01, run 49 of a 500-run `bun run test:everything -- --force` loop, 14 runs after the previous reset.
- Recurrences: 2 occurrences in 68 retained run directories for this test (~3%), both with an identical log shape.
- Root cause: **the test clicked Create while Create was still disabled, and a disabled button swallows the click in silence.** `create-database-modal.tsx` disables Create with `disabled={!form.path || nameError !== undefined}`, and `form.path` is only filled by `handleBrowse` after `await platform.pickFolder()` resolves. The test sent `click database-browse-button` and then `click create-database-confirm` with nothing waiting in between, and `doClick` returns as soon as it has dispatched the event, without waiting for the React handler. When the pick-folder promise and the re-render had not completed in the gap between those two commands, Create was still disabled, React ran no handler, and nothing at all was logged, so the test waited out its full timeout for a database that was never created. The fix makes the test wait for the path field to hold the browsed value before clicking Create, which is what a user cannot help but do and the test was skipping.
- Evidence: two failing runs (run-1702876 and run-2055182) whose app.log ends exactly at `test-click: clicking element data-id="create-database-confirm" nth=0`, against 66 passing runs which all continue `Database entry added`, `Database opened: db-one`, `Database created: db-one` on the very next lines. Nothing was logged between the click and the timeout in either failure, and no error was logged, which is what a swallowed click looks like: `handleCreate` logs on every path it can reach, so no log at all means it never ran.
- Note: `doClick` now warns when its target is disabled. It warns rather than throws because a test may legitimately click a disabled control to assert that nothing happens; the warning makes the swallow visible in app.log next to the click line.

### WAITFORELEMENT-LATE-RENDER-DEADLINE

- [x] Fixed and verified (10x clean). 169 passing runs since the fix landed at run 34 (runs 35 to 210 of the 2026-08-02 loop). None of the three tests failed in any of the 177 retained logs from that range, failed runs included.
- Suite: unit tests (`bun run test`), `packages/user-interface/src/test/lib/test-driver.test.ts`
- Pattern: `waitForElement ignores a hidden match and waits for a visible one`
- Same mode, other two tests built the same way: `resolves once an element that renders later appears`, `waits for the nth element when several share a data-id`
- Fix commit: packages/user-interface/src/test/lib/test-driver.test.ts, the three late-render tests now drive jest fake timers instead of wall-clock time (uncommitted working change; record the commit hash here once committed).
- First seen: 2026-08-01, run 34 of a 500-run `bun run test:everything -- --force` loop. Runs 1 to 33 passed.
- Recurrences: none since the fix.
- Root cause: **the test raced the machine, not the code.** The test renders a hidden match, schedules the visible one with `setTimeout(..., 80)`, then awaits `waitForElement("late", 0, 2000)`. `waitForElement` polls every 50ms against a wall-clock deadline (`Date.now() + timeoutMs`) and, when that deadline passes, returns silently rather than throwing. If the event loop stalls past the deadline while the poll is waiting, the resumption order decides the outcome: timers fire in order of expiry, so the 50ms poll timer runs before the 80ms render timer, and the poll's microtask continuation re-checks `Date.now() < deadline`, finds it false, and returns **while the render timer is still overdue and unrun**. The test then asserts against an unmutated DOM and sees 1 element instead of 2. Under the full parallel suite (6 lanes plus Android emulators and Electron) a multi-second stall of a jest worker is enough. The fix removes the dependence on wall-clock time: virtual time now drives both the poll loop and the render, so machine load cannot change the result. The real `waitForElement` still runs, and the tests were confirmed to go red when `isElementVisible` and `waitForElement` were each deliberately broken.
- Evidence: run 34 log, `● hidden elements are not actionable › waitForElement ignores a hidden match and waits for a visible one`, `Expected: 2`, `Received: 1` at `test-driver.test.ts:98`. Isolated 5x runs of the file idle and 5x under 3x CPU oversubscription all passed at ~103ms, so plain CPU contention is not the trigger. The ordering was reproduced directly in plain Node with the same loop shape: a 2100ms stall inside the first poll wait yields `returned-without-finding after 2150ms, mutationHappened=false`, the overdue render timer having still not run when the loop gave up.

### MCP-STARTUP-TIMEOUT-ABORTS-APP

- [ ] Fixed and verified (10x clean)
- Suite: Electron smoke tests (`bun run test:electron`), seen on test 16 (remove-recent-database) but not specific to it: any test in the suite can hit it, because the failure is in app startup and happens before the test's first action.
- Pattern: `MCP server failed to start within timeout`
- Cascade symptom (same mode, not a new entry): `Test control server did not report a listening port within \d+s`, and the app log ending at `MCP utility process spawned` with no `MCP server initialized in utility process on port` line after it
- Fix commit: none yet
- First seen: 2026-08-08, a 5-lane `bun run test:everything -- --force compile test test:cli test:electron test:and:unit` on the `parallel-test-interference` worktree immediately after rebasing onto `mobile` 38e3f7dd. 1 of 33 Electron tests failed. The same test then passed alone in 9s, and a full re-run of the identical 5-lane command passed all five scripts, so the mode is intermittent and load-dependent rather than a break introduced by the rebase.
- Recurrences: none yet
- Root cause: **a fixed 10-second startup timeout rejects under load, and nothing catches it, so the whole app stops starting.** `initMcpServer` in `apps/desktop/src/lib/mcp/main-bridge.ts:104` forks the MCP utility process and waits for it to answer `server-ready`, rejecting after a hardcoded 10000ms. `apps/desktop/src/main.ts:318` awaits that call inside `app.whenReady().then(async () => { ... })`, and the callback has no try/catch, so a rejection becomes an unhandled rejection and every remaining startup step is skipped: `createMenu`, `initWorkers`, `startPeriodicSync`, `createMainWindow`, and the test control server the smoke tests drive the app through. The suite then waits out its full 120s for a port that will never be bound. A slow MCP start should degrade the app's MCP feature, not prevent the window from opening. The trigger is machine load: the failing run had five suites in parallel, each Electron test spawning an app with three utility processes, and the S3 tests starting emulators alongside. Load rose on this tree because `mobile` grew the Electron suite from 26 to 34 tests (commit 0ee97673, "Added S3 smoke-test coverage across CLI, desktop and mobile"), but no MCP code changed and the 10s timeout has always been there, so the merge exposed the mode rather than causing it.
- Evidence: `/tmp/photosphere-tests/remove-recent-database-QJ8zCQ/app.log`, which records `REST API initialized in utility process on port 44979`, then `MCP utility process spawned`, then `Error: MCP server failed to start within timeout` at `bundle/main.js:121185` under `[ERROR] Unhandled rejection in main process`, then `Photosphere Desktop shutting down...`. The matching `test-run.log` ends `[FAIL] Test control server did not report a listening port within 120s` with no test action ever executed. Re-running `bash ./smoke-tests.sh remove-recent-database` from `apps/desktop` passed in 9s.

### S3-LOCK-BROKEN-WHILE-HELD

- [ ] Fixed and verified (10x clean)
- Suite: CLI smoke tests (`bun run test:cli`), test 76 (s3-write-locks)
- Pattern: `\w[\w.-]* survived the concurrent writes \(pattern '[^']+' not found in output\)`
- Fix commit: recorded once a repeated clean run of the suite backs it up
- First seen: 2026-08-10, `bun run find-flakey-tests -- --ladder --target 10`, session `tmp/find-flakey-tests/20260810-200614`, rung `test:cli` run 2 of 10. The rung below it (`bun run test`) had already banked its full 10 green runs.
- Recurrences:
  - 2026-08-10, 6 concurrent copies of test 76, run 4 of a 40-run loop, session `tmp/find-flakey-tests/20260810-212217`. Lost `test-1.jpeg`.
  - 2026-08-10, the same loop with verbose lock logging on, session `tmp/find-flakey-tests/20260810-213328`. Lost `test-1.jpeg`, and this is the run the lock timeline below comes from.
- Root cause: **a live S3 write lock was mistaken for a corrupt one and broken, letting three processes write the database at once.** `CloudStorage.acquireWriteLock` takes the lock with a conditional `PutObject` carrying `IfNoneMatch: "*"`, which is sound. When that write came back 412 it read the lock object back to decide whether the holder had timed out, and when the read returned nothing it concluded the lock file was corrupt, deleted it, and wrote its own with `IfNoneMatch` removed. The lock was not corrupt: the read had raced its owner, who was still in the critical section and released 11ms later. Deleting it also cleared the way for a third process's conditional write to succeed. Test 76 runs four `psi add` processes against one database, so two of them then wrote the BSON shard files, both sort index leaf pages and the merkle tree together, and one writer's records were overwritten. The record survives in its own shard file, because each record hashes to its own shard, and disappears from the shared `photoDate_desc` sort index leaf, which is what `psi list` pages through, so the file vanishes from the listing while `psi verify` still passes.
- Evidence: the lock timeline from the four writers of `/tmp/photosphere-tests/76-s3-write-locks-wS8L93`, times in ms and pids being the four concurrent CLI processes. `857366 pid 31715 ACQUIRE_SUCCESS`, `857384 pid 31712 ACQUIRE_FAILED_EXISTS`, `857502 pid 31713 ACQUIRE_FAILED_EXISTS (age 136ms)`, `857602 pid 31713 ACQUIRE_CORRUPTED_BREAK`, `857602 pid 31713 ACQUIRE_SUCCESS_AFTER_CORRUPT`, `857607 pid 31712 ACQUIRE_SUCCESS`, `857613 pid 31715 RELEASE_SUCCESS`. Three processes held the lock together between 857602 and 857613, and pid 31712 was the writer adding `multiple-files/test-1.jpeg`, the file the assertion reported missing. An earlier instance shows the same overlap from the other side: the lost record was present in the `hash_asc` index leaf and absent from the `photoDate_desc` leaf, and those two leaves are written back to back inside one commit, so another writer's commit landed between them. Test 76 run on its own passed 30 of 30 and the whole CLI suite on its own passed 15 of 15, so the mode needs several writers contending at once rather than load alone.
- Known hole left alone: breaking a lock that has genuinely aged past the timeout still deletes it and then writes without `IfNoneMatch`, so a lock another process takes in that gap is overwritten. That is the same defect by a second route, but nothing here has been seen to reach it, it needs a lock older than 10 seconds, and it is not what this fix is for.
- Note: test 76 also hits a separate mode where Bun itself dies during `psi verify` with `panic: Unexpected JS error: JSError` and `terminated by signal SIGILL`, seen twice on 2026-08-10. That is a Bun crash rather than a lost update and is not this entry.

### MOBILE-LAN-RECEIVER-NEVER-DISCOVERED

- [ ] Fixed and verified (10x clean). Not fixed: the cause is not established. The change recorded below only makes the next occurrence readable.
- Suite: mobile smoke tests (`bun run test:and`), any test where the device receives over the LAN. Seen on 44 (receive-database-cancel) and 26 (receive-database).
- Pattern: `Timed out waiting for log pattern: Database review step`
- Also matches (same mode, seen from the sender's side): `The CLI sender did not report a successful transfer` with `No device found within 60 seconds` in the sender log
- Fix commit: none
- First seen: 2026-08-11, the `.githooks/pre-commit` run of `bun run test:everything`, test 44, 1 of 43 mobile tests failed.
- Recurrences: repeatedly on 2026-08-11 while the machine was busy, on both test 44 and test 26, at rates between 1 run in 6 and 1 run in 1. Sessions `tmp/find-flakey-tests/20260811-065301`, `20260811-070...` onwards.
- Root cause: **not established.** What is measured: the device's receive task runs to its full 60-second timeout and returns a null payload, the engine logs no error and no failed task (logcat, tags `JsEngineQuickJs`/`JsEngineHost`), and the host sender reports either that it found no device at all or that it delivered successfully to something that was not this receiver. So the receiver believes it is healthy while its announcements do not reach the host.
- Ruled out, each by measurement rather than argument:
  - A late cancel. The cancelled receive's task settles 389-447ms after the click, seconds before the sender starts, measured in three separate failures.
  - The second receive being cancelled by the first. It runs its full 60s (60590-60866ms measured).
  - Anything specific to cancelling. Test 26 has no cancel in it and fails identically.
  - A pairing-code collision. `allocate_pairing_code` is a bare `RANDOM % 9000` with no uniqueness check, which is worth fixing on its own, but 12 concurrent draws came back all distinct and the codes in the failing runs were unique to those runs.
  - Bun's ICMP-closes-the-socket behaviour. Android's `UdpHost.udpSend` returns an error envelope and leaves the socket open.
- Load dependence: with the machine quiet, test 26 passed 50 consecutive runs (20 with a host listener bound to the discovery port, 20 without, plus an earlier 10). Every failure so far happened while a full parallel suite or several loops were running. That is why nothing here is proven: the mode cannot currently be reproduced on demand.
- Two silent paths found on the way, neither shown to cause this, both worth knowing: `UdpHost.udpSend` returns success when the socket id is unknown, and `LanShareReceiver` swallows every UDP discovery error on the grounds that discovery is best-effort, when a receiver that never advertises can never be reached. The embedded JS engine the receiver runs in has no logging wiring at all, so nothing in that code can report anything.
- Evidence: `/tmp/photosphere-tests/44-receive-database-cancel-{upHBI4,NMC255,GP7qMA,7RklsN,HoyQVP}` and `26-receive-database-fe0RSB`. The app log of every one ends at the Start click with, after the change in this commit, `Receive gave up after 60602ms with no payload`.
