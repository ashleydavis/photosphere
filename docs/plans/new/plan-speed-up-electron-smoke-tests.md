# Speed up the Electron smoke tests

## Overview

`bun run test:electron` takes 6m 10s on this machine (34 tests, all passing, measured 2026-08-12 on 24 cores / 62GB). The target is under 3 minutes. The app is not what is slow: a measured Electron launch reaches its test control port in 1.16s and answers `/ready` 89ms later, and it exits 1.13s after `/quit`. The suite is slow because of the harness around the app. Three things account for most of it. Every polling loop in `apps/desktop/smoke-tests/lib/common.sh` and in the tests' own copies of those loops sleeps a whole second between looks, so roughly 300 waits each pay up to a second of latency for an event that already happened. Every teardown sleeps a fixed 3 seconds (`stop_app`'s `sleep 2` plus `kill_process_group`'s 1 second signal grace) to stop a process that takes 1.13s to go, and the exit trap then pays the grace again. And `run_parallel_batch` in `apps/desktop/smoke-tests.sh` runs tests two at a time behind a barrier, so a batch takes as long as its slower member and nothing starts until both are done: test 28 ran for 1m 2s while its partner sat finished for 42 seconds of it.

The measured serial cost of all 34 tests is 599s against a wall clock of 370s, so the suite is only getting 1.6x out of a 24 core machine. This plan removes the fixed sleeping, drops the poll interval, replaces the batch barrier with a rolling pool, and takes the pool width from the core count instead of the hardcoded 2. The changes are entirely shell, in the harness and the tests, with no change to the app.

## Issues

## Steps

1. **Add a process group liveness check and make the kill helpers poll, in `scripts/lib/process-control.sh`.** Add `PROCESS_CONTROL_POLL_INTERVAL=0.1` beside `PROCESS_CONTROL_TERM_GRACE_SECONDS` at line 32, and two functions, each with a `//`-style comment block matching the file's existing voice:
   - `process_group_alive <pgid>` returns 0 when at least one process is still in the group. `kill -0 -- "-$pgid"` is the check, verified to work here: it succeeds while any member lives and fails once the group is empty. Nothing this library launches can become a zombie that keeps answering, because `launch_in_process_group` runs inside the process substitution in `start_app`, so the app is reparented to init and reaped there rather than left unwaited in the test's shell.
   - `wait_for_process_group_exit <pgid> <timeout_seconds>` polls `process_group_alive` every `PROCESS_CONTROL_POLL_INTERVAL` until the group is empty or the timeout passes, returning 0 when it went and 1 when it is still there.
   Then change `kill_process_group` (line 102) so that after the `SIGTERM` it calls `wait_for_process_group_exit "$pgid" "$PROCESS_CONTROL_TERM_GRACE_SECONDS"` in place of the flat `sleep`, and skips the `SIGKILL` entirely when the group has already gone. Return early, before signalling anything, when `process_group_alive` is already false. Make the same change in `kill_process_tree` (line 73), polling the collected pid list with `kill -0` instead of sleeping the grace out. The worst case is unchanged: a process that ignores `SIGTERM` still gets the full grace and then `SIGKILL`. Only the common case, where everything is already gone, gets faster.

2. **Stop `stop_app` sleeping for an exit it can watch for, in `apps/desktop/smoke-tests/lib/common.sh` (line 519).** After posting `/quit`, read `$tmp_dir/app.pgid` and call `wait_for_process_group_exit` with a timeout of 5 seconds instead of `sleep 2`, then call `cleanup_apps` as it does now. With step 1 in place `cleanup_apps` returns immediately when the app has already gone, so a clean shutdown costs the measured 1.13s instead of 3s, and a wedged app still gets stopped exactly as it is today. Fall back to the current `sleep 2` only when no pgid file exists, which means `start_app` never got as far as writing one.

3. **Introduce one poll interval for the desktop waits, in `apps/desktop/smoke-tests/lib/common.sh`.** Add `WAIT_POLL_INTERVAL=0.25` beside `DEFAULT_WAIT_TIMEOUT` (line 28) with a comment saying why it is not 1 and not 0.01: it is what makes a wait cost about an eighth of a second rather than half a second on average, without putting four `awk` or `curl` processes per second per test on the machine more than the pool width allows. Rewrite the loops in `wait_for_test_port` (line 176), `wait_for_ready` (line 346), `wait_for_log` (line 397) and `wait_for_value` (line 455) to run to a deadline computed from bash's `SECONDS` (`local deadline=$((SECONDS + timeout))`, `while [ "$SECONDS" -lt "$deadline" ]`) and to sleep `WAIT_POLL_INTERVAL` between looks. The deadline form is what keeps the declared timeouts honest: counting polls would stretch a 30 second timeout into something longer whenever a poll's own work is slow, which `wait_for_log`'s `awk` over a growing log can be. Every timeout value, every log message and every failure path stays exactly as it is; only the interval and the counter change.

4. **Add the two missing shared waits to `apps/desktop/smoke-tests/lib/common.sh`,** both using `WAIT_POLL_INTERVAL` and the deadline form from step 3, both with the comment block the file's other helpers have:
   - `wait_for_value_gone <port> <data-id> <substring> [timeout]` waits until the element's value no longer contains the substring, requiring a non-empty response first so a dead app cannot read as a closed dialog. Lift the body from the local copy in `23-developer-screen/test.sh` (line 42), which already gets this right and explains why.
   - `read_pairing_code <port> [timeout]` polls `share-pairing-code` until it reads a four digit code, prints it on stdout and returns non-zero when none appeared. Lift the body from `29-share-database-cancel/test.sh` (line 37).

5. **Delete the tests' private copies of those loops and call the shared ones.** Seventeen `sleep 1` polls live inside per-test functions, and each is a copy of a helper that already exists or is added in step 4. Changing the interval in one place is worth nothing while these copies exist, so remove them:
   - `23-developer-screen/test.sh` (lines 18 to 60) and `24-sync-settings/test.sh` (lines 29 to 60): local `wait_for_value` and `wait_for_value_gone` definitions identical to the shared ones apart from a 30 second timeout. Delete both definitions and pass `30` as the timeout argument at each call site.
   - `7-share-secret/test.sh` (line 38), `8-share-database/test.sh`, `29-share-database-cancel/test.sh` (line 37) and `31-share-secret-cancel/test.sh` (line 33): replace the inline pairing code loops with `read_pairing_code`, keeping each test's own failure message.
   - `7-share-secret/test.sh` (line 80) and `8-share-database/test.sh` (line 96): the "row appears behind the success dialog" loops become `wait_for_value "$RECEIVER_PORT" <data-id> <expected> 10`. Keep the explanatory comment above each, since it says what the assertion is for.
   - `30-receive-database-cancel/test.sh` (lines 63 and 95), `32-receive-secret-cancel/test.sh` (lines 50 and 82) and `33-import-cancel/test.sh` (line 85): the "value is no longer Cancel" loops become `wait_for_value_gone`, with each test's own timeout and failure message preserved.
   - `31-share-secret-cancel/test.sh` (line 75) waits for the pairing code to stop being a four digit code, which is a pattern test rather than the substring test `wait_for_value_gone` does. Leave its condition alone and change only its `sleep 1` to `WAIT_POLL_INTERVAL` and its counter to a `SECONDS` deadline, so it does not quietly become a weaker assertion.

6. **Race the two outcomes in `apps/desktop/smoke-tests/28-s3-failure/test.sh` (lines 100 to 130) instead of waiting one out.** This is the slowest test in the suite at 1m 2s, and 30 of those seconds are a `wait_for_log` for a line that must never appear. Replace that block and the 90 second error poll that follows it with a single loop that polls `app.log` every `WAIT_POLL_INTERVAL` for up to `ERROR_WAIT_SECONDS` and stops at whichever lands first: the expected `Could not reach the database at ...` line passes the test, and `Load assets task completed: 0 assets loaded` fails it with the message that block already carries. Reaching the timeout without either fails with the existing "never logged" message. Nothing is weakened: both outcomes are still checked, and the failing one is still the one that fails the test. What goes is the fixed 30 seconds spent proving an absence that the successful outcome already disproves. Measured from the baseline run, the error line arrives about 20 seconds after the click, so this test should land near 30s.

7. **Add `scripts/lib/test-concurrency.sh`.** A new sourced library beside `allocate-test-temp-dir.sh` and `process-control.sh`, defining `detect_cpu_count` and `resolve_test_parallel <fallback>` exactly as `docs/plans/new/plan-speed-up-test-everything.md` steps 3 specifies them, so the two plans converge on one library rather than two:
   - `detect_cpu_count` prints the usable core count from `getconf _NPROCESSORS_ONLN`, falling back to `nproc`, then to `sysctl -n hw.ncpu`, then to 4, so an unknown host gets a sane number rather than an empty string.
   - `resolve_test_parallel <fallback>` prints `PHOTOSPHERE_TEST_PARALLEL` when it is set to a positive integer, refusing loudly and returning non-zero when it is set to anything else. Otherwise it prints `detect_cpu_count / 4`, clamped to at least the given fallback and at most 6. The divisor and cap are what keep this safe beside the other suites: `scripts/test-everything-parallel.sh` runs this suite in one lane of ten, and a suite sizing itself to all 24 cores would make that run slower rather than faster.
   - Written for the bash macOS ships (3.2): no associative arrays, no `mapfile`, no `${var,,}`.

8. **Replace the batch barrier with a rolling pool, in `apps/desktop/smoke-tests.sh`.** Rewrite `run_parallel_batch` (line 156) as `run_parallel_pool`, keeping its `<n> <pass_var> <fail_var> <test...>` interface so `run_mixed` (line 230) needs only its call updated. The pool keeps at most `n` tests in flight and starts the next the moment any slot frees, rather than waiting for a whole batch. Hold three parallel indexed arrays (`pool_pids`, `pool_tests`, `pool_temp_dirs`) of size `n`, and loop: fill every empty slot from the remaining tests, then poll the occupied slots every 0.1s with `kill -0`, and when one has gone, `wait` that pid for its exit status, print its PASS or FAIL or TIMEOUT line exactly as the current code does, append to `FAILED_TEST_LOGS` on failure, and empty the slot. Use indexed arrays and a `kill -0` poll rather than `wait -n` or `declare -A`, because `wait -n` does not exist in bash 3.2. Results print as tests finish rather than in test order, which is what the batch runner already does within a batch.

9. **Take the pool width from the core count, in `apps/desktop/smoke-tests.sh`.** Source `scripts/lib/test-concurrency.sh` beside the existing sources at lines 9 and 14, and set `local parallel_n` in `main()` (line 372) from `resolve_test_parallel 2` instead of the literal `2`. An explicit `--parallel N` on the command line still wins over both. Update `print_usage` (line 65) to say the default comes from the core count and name `PHOTOSPHERE_TEST_PARALLEL`. Leave `run_mixed`'s handling of `.sequential` alone: `7-share-secret` and `8-share-database` still run one at a time after the pool has drained, because the reason they are marked (LAN broadcast interference between two full share flows) is unaffected by how the rest of the suite is scheduled.

10. **Poll the S3 emulator's health check faster, in `scripts/s3-emulator.sh` (line 251).** Four Electron tests and several CLI suites start a MinIO server and wait for it at 1 second granularity. Change that loop's `sleep 1` (line 263) to `sleep 0.25` and the `elapsed` counter to a `SECONDS` deadline, leaving `HEALTH_TIMEOUT_SECONDS` at 60. Do not touch the seed retry loop at line 358: its 1 second delay is a backoff between attempts at a server that has answered but is not ready, not a poll for an event.

11. **Replace the three fixed settle sleeps that are waiting for a screen, not proving an absence.** `3-open-database/test.sh` line 43, `16-remove-recent-database/test.sh` line 45 and `34-edit-asset-metadata/test.sh` line 105 each `sleep 1` for a drawer or a dialog to mount before clicking into it. Replace each with `wait_for_value` on the very element the next line clicks, which is both faster and removes the race the comment above each sleep is describing. Leave `17-news-notifications/test.sh` line 122 (`sleep 3`) and `33-import-cancel/test.sh` line 107 (`sleep 5`) exactly as they are: both are settling periods before asserting that something did not happen, and there is no event to wait for instead without adding a log line to the app for the tests' benefit.

12. **Give the three tests that name `/tmp/smoke-test-db` a path of their own.** `8-share-database/test.sh` line 30, `29-share-database-cancel/test.sh` line 30 and `30-receive-database-cancel/test.sh` line 28 each write a `databases.toml` entry pointing at the fixed machine-wide path `/tmp/smoke-test-db`. Nothing creates it today, so it is a listing entry rather than a database, but it breaks the repository's rule that no test may claim a machine-wide resource by a fixed name, and the wider pool from step 8 puts more of these tests on the machine at once. Point each at `$TMP_DIR/smoke-test-db` instead.

13. **Update `apps/desktop/README.md` (the Shell Smoke Tests section, from line 98).** Say that the suite runs a rolling pool sized from the core count, that `--parallel N` and `PHOTOSPHERE_TEST_PARALLEL` override it, and that `.sequential`-marked tests still run alone at the end. Replace the "parallel batches" wording, which will no longer be true.

Every step above is complete only when `bash -n` parses the files it changed and `bun run test:electron` still reports all 34 tests passing. Steps 1 and 2 change shared process handling, so run `bun run test:cli` after them as well, since the CLI suites source the same `process-control.sh`.

## Unit Tests

None, deliberately. Every change in this plan is shell script, and `CLAUDE.md` states that tests are not to be written for shell scripts, that no `*.test.sh` files are to be created, and that the rule requiring a unit test for every new or changed function does not apply to shell. So `scripts/lib/test-concurrency.sh`, the new `process_group_alive` and `wait_for_process_group_exit`, and the new `wait_for_value_gone` and `read_pairing_code` get no test files, and nothing is added beside the existing `apps/smoke-tests/runner.test.sh`, `android-lock.test.sh` and `timeout.test.sh`. What proves this work is the real suites, run repeatedly, in the Verify section. No app code changes, so there is no TypeScript function here to test.

## Smoke Tests

The Electron suite is itself the end-to-end coverage for all of this, and every one of its 34 tests exercises the changed helpers. The changes add a few properties worth checking directly, each a single command:

- `bun run test:electron` reports all 34 tests passing and a Duration under 3 minutes.
- `PHOTOSPHERE_TEST_PARALLEL=3 bash apps/desktop/smoke-tests.sh ls` and a full run at that setting show three tests in flight at once and still pass.
- `PHOTOSPHERE_TEST_PARALLEL=0`, `-1` and `abc` are each refused loudly with a non-zero exit rather than silently treated as one or as unset.
- `bash apps/desktop/smoke-tests.sh --parallel 2` still works and beats the environment variable, so the old behaviour is reachable when narrowing down a failure.
- `bash apps/desktop/smoke-tests.sh 28` runs test 28 alone and passes, and its own reported duration is near 30s rather than the baseline 1m 2s.
- `bash apps/desktop/smoke-tests.sh --sequential` still runs every test one at a time and passes.
- A run that is interrupted with Ctrl-C, and a run in which one test is made to fail on purpose, both still end with the leak check reporting nothing left running. The pool must not lose track of a slot's process group.
- `bun run test:cli` passes, since `process-control.sh` and `s3-emulator.sh` are shared with it.

## Verify

- `bun run compile` succeeds (no TypeScript changes are expected, so this is a guard against an accidental one).
- `bash -n` parses cleanly for `scripts/lib/process-control.sh`, `scripts/lib/test-concurrency.sh`, `scripts/s3-emulator.sh`, `apps/desktop/smoke-tests.sh`, `apps/desktop/smoke-tests/lib/common.sh` and every `test.sh` changed in steps 5, 6, 11 and 12.
- `bun run test` passes.
- `bun run test:electron` passes with all 34 tests green, and its reported Duration is under 3 minutes. Record the number and compare it against the 6m 10s baseline in the Notes below.
- `bun run test:electron` is run three times in a row, all green, before the work is called done. Raising concurrency is exactly the change that turns a stable suite flaky, and one green run does not distinguish the two.
- `bun run test:parallel` passes, which runs each suite alone and then every pair together, including each suite against a second copy of itself. This is what catches the Electron suite becoming a bad neighbour at the wider pool width.
- `bun run test:everything -- --force` passes with every lane green, so the wider pool is shown to behave when the machine is also running the CLI, mobile and compile lanes.
- Every check in the Smoke Tests section passes.

## Notes

- **The measurements this plan rests on**, all taken on this machine on 2026-08-12 with nothing else running:
  - Baseline: `bun run test:electron`, 34 tests, all passed, 6m 10s. The serial sum of the per-test durations was 599s, so the suite got 1.6x out of 24 cores.
  - `bun run bundle` takes 5.5s, so the build is not worth attacking.
  - Electron launch to its control port file: 1156ms. Port file to `/ready`: 89ms. The suite pays about 2.0s for that because both waits poll at 1 second.
  - `/quit` to the process group being empty: 1134ms. The suite pays 3s in `stop_app` and about 1s more in the exit trap.
  - The suite makes 48 app launches, 44 `stop_app` calls and 34 exit-trap cleanups, and its waits number 172 `wait_for_log`, 48 `wait_for_ready`, 35 `wait_for_value` and 48 `wait_for_test_port`, plus 17 private copies inside the tests.
  - The slowest tests were 28-s3-failure at 1m 2s, 26-s3-database-lifecycle at 31s, 17-news-notifications at 27s, and 8-share-database at 24s.
- **Where the 3 minutes is expected to come from.** Teardown polling instead of sleeping removes about 120s of the 599s serial cost, the quarter second poll interval about 96s, and step 6 about 30s, leaving roughly 355s. Of that, the two `.sequential` tests are about 35s that cannot overlap anything, so about 320s spreads across a pool of 6 for about 55s, bounded below by the longest single test at about 30s. With the 5.5s bundle that is a little under 1m 40s. Even at a pool of 4, or if the estimates are half right, the 3 minute target holds. These are estimates from measured parts, not a measured result.
- **Fractional sleeps.** `sleep 0.25` is accepted by GNU coreutils (Linux, and the coreutils Git Bash ships) and by the BSD `sleep` on macOS, so no fallback is needed. If a host is ever found that refuses one, that is a loud immediate failure in every wait rather than a silent slow path.
- **What is deliberately not touched.** `xvfb-run` costs 78ms per launch, measured, so the idea of one shared X server for the whole suite would buy nothing and would complicate the process group cleanup that the current design is careful about. The `.sequential` marking of tests 7 and 8 stays: overlapping them with the pool would claw back about 35s of tail, but they are two full LAN share flows and the marker exists because those interfere, and the target is met without taking that risk. The `sleep 3` in test 17 and the `sleep 5` in test 33 stay, because both are proving that something did not happen and replacing them means adding a log line to the app for the tests' benefit, which needs asking about first.
- **Overlap with `docs/plans/new/plan-speed-up-test-everything.md`.** That plan proposes the same `scripts/lib/test-concurrency.sh` with the same two functions, and its step 5 does what step 9 here does. Whichever is implemented first creates the library; the other then finds its step already done. The environment variable name is kept as `PHOTOSPHERE_TEST_PARALLEL` so that plan's step 6, which divides a whole-run concurrency budget across lanes, can set this suite's pool width without any further change here.
- **The CLI suite has the same batch barrier**, at `apps/cli/smoke-tests.sh:528`, with `PARALLEL_N=5` over 79 tests. It is out of scope here, but `run_parallel_pool` from step 8 is the same rewrite it needs, and its baseline is 206s.
- **Stale in-tree `tmp/` directories** are left behind under several test directories (for example `apps/desktop/smoke-tests/8-share-database/tmp/`) from before per-test temporary directories were introduced. They are not read by anything now. Removing them is not part of this plan.
