# Converge Electron Smoke Tests onto the Host Control Bridge

## Overview
The mobile smoke tests drive the app from the host over a WebSocket: a host-side control bridge (`apps/smoke-tests/lib/control-bridge.ts`) exposes the HTTP command surface, relays each command to the app's WebView over a WebSocket, and the shared DOM driver (`packages/user-interface/src/lib/test-driver.ts` + `test-driver-ws.ts`) executes it. Electron still uses the older model: an in-process HTTP server (`apps/desktop/src/lib/test-control-server.ts`) compiled into the app, driven over Electron IPC. This plan converts Electron to the same host-bridge model so there is one shared smoke-test suite and one driver for all shells. The payoff: no test-only HTTP control server inside the shipped Electron app (a security win), a single set of test bodies instead of the current desktop/mobile duplication, and the bridge/driver proven across desktop + mobile (and ready for any future shell). The work re-homes the three Node-only capabilities the in-process server provides (window screenshot via `capturePage`, app quit, and any worker-pool task ops) onto the bridge/driver, then migrates the desktop test bodies into the shared `apps/smoke-tests` harness behind a new `electron` platform and retires the in-process server.

## Issues
<!-- Populated later by plan:check -->

## Constraints
- **No re-homing without explicit approval.** Do not move, relocate, or change where any Node-only / main-process capability runs — specifically the window screenshot (`capturePage`), app quit (`app.quit`), and worker-pool task operations — without the user's explicit approval first. These must stay in the Electron main process unless the user approves otherwise.
- The intended design keeps those capabilities in the main process and only re-exposes them to the renderer over IPC (steps 3 and 6); it does not move the logic out of main. If any step would actually relocate one of these capabilities, stop and get approval before implementing it.
- This constraint overrides any step below: if executing a step appears to require re-homing a capability, pause and ask rather than proceed.

## Steps

1. Extend the shared driver to accept platform-specific command handlers. In `packages/user-interface/src/lib/test-driver.ts`, change `installTestDriver(transport: ITestTransport)` to `installTestDriver(transport: ITestTransport, platformHandlers?: ITestPlatformHandlers)` where `ITestPlatformHandlers` is a new named interface mapping a command name to an async handler returning `string | undefined` (used for `screenshot` and `quit`, which need shell-native capability). In the command switch, before the `default` throw, consult `platformHandlers[command]` and call it if present. Keep all existing DOM cases unchanged. Requirement: package type-checks; add/extend unit tests (step 13) and they pass.

2. Thread platform handlers through the WebSocket client. In `packages/user-interface/src/lib/test-driver-ws.ts`, change `connectTestDriverWebSocket(url: string)` to `connectTestDriverWebSocket(url: string, platformHandlers?: ITestPlatformHandlers)` and pass them to `installTestDriver`. The reply path already serialises a returned `string` as `value`; a `screenshot` handler returns base64 PNG text, which the bridge writes to disk. Requirement: type-checks; unit tests (step 13) pass.

3. Add a main-process screenshot + quit IPC surface for Electron. In `apps/desktop/src/main.ts`, register `ipcMain.handle('test-capture-page', ...)` that calls `mainWindow.webContents.capturePage()` and returns the PNG as a base64 string, and `ipcMain.on('test-quit', () => app.quit())`. In `apps/desktop/src/preload.ts`, expose `capturePage(): Promise<string>` (invoke `test-capture-page`) and `quit(): void` (send `test-quit`) on `electronAPI`. In `apps/desktop-frontend/src/lib/electron-ipc.ts`, add `capturePage` and `quit` to `IElectronAPI`. Requirement: desktop + desktop-frontend type-check.

4. Add the test-driving window-event listeners to the Electron platform provider. In `apps/desktop-frontend/src/lib/platform-provider-electron.tsx`, add a `useEffect` that listens for `TEST_MENU_EVENT` and `TEST_OPEN_DATABASE_EVENT` (imported from `user-interface`) and fires the existing `menuActionCallbacksRef` and `openedCallbacksRef` respectively — mirroring `PlatformProviderMobile`. This lets the shared driver's `doMenu`/`doOpenDatabase` drive Electron without the in-process server. Leave the existing IPC `menu-action`/`navigate`/`database-opened` listeners in place (real app menu still uses them). Requirement: covered by the e2e suite (step 15); component is not unit tested.

5. Switch the Electron renderer from the IPC transport to the WebSocket client. In `apps/desktop-frontend/src/index.tsx`, in the `isTestMode` block, replace the Electron-IPC `ITestTransport` + `installTestDriver` call with a call to `connectTestDriverWebSocket(\`ws://localhost:${bridgePort}\`, platformHandlers)`, where `bridgePort` is read from a new `testBridgePort` URL query parameter and `platformHandlers` is `{ screenshot: async () => electronAPI.capturePage(), quit: async () => { electronAPI.quit(); return undefined; } }`. Remove the now-unused IPC transport. Keep the existing renderer console patch (or rely on the WS client's `patchConsole`; pick one and remove the duplicate). Requirement: desktop-frontend type-checks; e2e suite (step 15) passes.

6. Pass the bridge port to the renderer and stop creating the in-process server. In `apps/desktop/src/main.ts`: append `&testBridgePort=${process.env.PHOTOSPHERE_TEST_PORT}` to the `fileUrl` when `testMode` is set (the harness sets `PHOTOSPHERE_TEST_PORT` to the bridge port); delete the `TestControlServer` import, the `testControlServer` variable, its construction (around line 324), and the `testControlServer.notifyReady()` call (around line 150). Readiness is now signalled by the renderer sending `ready` over the WebSocket. Requirement: desktop type-checks; e2e suite passes.

7. Teach the control bridge an `electron` platform for screenshot and quit. In `apps/smoke-tests/lib/control-bridge.ts`, in `handleScreenshot`, add an `electron` branch that forwards a `screenshot` command to the app over the WebSocket (via `sendCommand`) and writes the returned base64 `value` to `outputPath` (decode base64 → bytes). In `handleQuit`, add an `electron` branch that forwards a `quit` command to the app. Keep the existing `android`/`ios` host-side branches. Requirement: package type-checks; unit tests (step 14) pass.

8. Add the Electron platform launcher to the shared harness. Create `apps/smoke-tests/lib/electron.sh` with: `electron_setup_env` (no-op or resolve the electron binary), `electron_prepare` (verify the electron binary / xvfb availability), `electron_build` (bundle main + renderer via `bun run bundle` equivalents used today), `electron_install` (no-op), `electron_launch <port>` (launch the Electron app headlessly under `xvfb-run` on Linux with `PHOTOSPHERE_TEST_MODE=1 PHOTOSPHERE_TEST_PORT=<port>` and the other env the desktop `start_app` sets today, writing pid to `$tmp_dir/app.pid`), and `electron_stop <port>` (kill the pid). In `apps/smoke-tests/lib/common.sh`, extend `load_platform` to source `electron.sh` when `PLATFORM=electron`. The bridge host for Electron is `localhost` with no port forwarding. Requirement: `bash -n` passes on all shell files.

9. Make `start_app` honour an app that the launcher manages by pid (Electron) vs by device (mobile). In `apps/smoke-tests/lib/common.sh`, ensure `start_app` starts the bridge, then calls `${PLATFORM}_launch`; for Electron the launched process pid is recorded by `electron_launch`. Ensure `stop_app` calls `${PLATFORM}_stop` and then kills the bridge, and that Electron's `/quit` (forwarded to the app) plus `electron_stop` are both safe/idempotent. Requirement: `bash -n` passes.

10. Introduce a platform seeding abstraction for tests that need pre-existing data. In `apps/smoke-tests/lib/common.sh` (or a new `lib/seed.sh` sourced by it), define abstract helpers the test bodies call instead of inline host seeding: `seed_database <tmp_dir> <name> <path> [fixture]`, `seed_secret <tmp_dir> <name> <type> <value>`, and `seed_databases_config <tmp_dir> <toml-body>`. Implement them per platform: `electron.sh` provides real implementations (CLI `init`, write vault JSON, write `databases.toml` on the host filesystem, exactly as the current desktop tests do); `android.sh`/`ios.sh` provide implementations that currently `log_info` "seeding not available on mobile yet" and return non-zero or set a skip marker (so seeding-dependent tests xfail cleanly on mobile until mobile storage lands). Requirement: `bash -n` passes.

11. Migrate the desktop test bodies into the shared suite. For each scenario under `apps/desktop/smoke-tests/<n>-<name>/test.sh`, update the corresponding `apps/smoke-tests/tests/<n>-<name>/test.sh` to be the single shared body: source `../../lib/common.sh`, use the `seed_*` helpers (step 10) for setup, drive the app with `send_command` exactly as today, and guard host-filesystem assertions behind `if [ "$PLATFORM" = "electron" ]` (electron can verify host files; mobile cannot). Restore the assertions that were dropped in the mobile-only port where the seeding abstraction now makes them runnable on electron. Keep `0-launch-and-navigate` as a mobile/electron-agnostic smoke. Requirement: shell files `bash -n` clean; suite runs (steps 15–16).

12. Retire the in-process server and old desktop harness. Delete `apps/desktop/src/lib/test-control-server.ts` and its test file. Delete `apps/desktop/smoke-tests/` (the per-test dirs and `lib/`) and `apps/desktop/smoke-tests.sh` once parity is confirmed (step 16). In the root `package.json`, repoint `test:electron` to run the shared harness (`cd ./apps/smoke-tests && PLATFORM=electron ./run.sh`) and keep `test:electron:seq` equivalent if still needed. Requirement: repo compiles; `grep` shows no remaining references to `TestControlServer` or the old paths.

13. Unit-test the driver changes. Update `packages/user-interface/src/test/lib/test-driver.test.ts` to cover `installTestDriver` with `platformHandlers`: a provided `screenshot` handler is invoked and its value returned; a provided `quit` handler is invoked; an unknown command with no platform handler still throws. Requirement: tests pass.

14. Unit-test the bridge electron branches. Update `apps/smoke-tests/src/test/control-bridge.test.ts` to cover: with `platform: 'electron'`, `/screenshot` forwards a `screenshot` command and writes the decoded base64 reply to the output path; `/quit` forwards a `quit` command. Use the existing fake-WebSocket-client test harness. Requirement: tests pass.

15. Add/confirm an Electron run of the shared suite. Ensure `bun run test:electron` (now the shared harness, `PLATFORM=electron`) runs `apps/smoke-tests/tests/*` headlessly and passes for every scenario that passed under the old desktop harness. Requirement: all migrated scenarios pass under electron.

16. Confirm parity and unaffected mobile. Compare the electron pass set against the pre-migration desktop pass set (should match). Re-run `bun run test:android` and confirm its pass/fail set is unchanged by the shared-suite refactor (the `seed_*` mobile stubs keep seeding-dependent tests failing for the same real reasons). Requirement: no regressions.

## Unit Tests
- `installTestDriver` (test-driver.ts): platform handler for `screenshot` is invoked and its return value is propagated; platform handler for `quit` is invoked; unknown command without a platform handler throws "not implemented".
- `connectTestDriverWebSocket` (test-driver-ws.ts): when constructed with `platformHandlers`, a received `screenshot` command resolves to a reply whose `value` is the handler's return; existing command/reply/log/ready tests still pass.
- `ControlBridge` (control-bridge.ts): `platform: 'electron'` `/screenshot` forwards a `screenshot` command and writes the decoded base64 reply to `outputPath`; `platform: 'electron'` `/quit` forwards a `quit` command; existing android/ios and forwarding tests unchanged.
- Main-process `test-capture-page` handler: not unit tested directly (Electron main); covered by the electron e2e screenshot step instead.

## Smoke Tests
- `apps/smoke-tests/tests/*` run with `PLATFORM=electron` headlessly (xvfb on Linux): every migrated desktop scenario passes, including a scenario that calls `/screenshot` (verifies the app-returned base64 image is written) and one that calls `/quit`.
- The same `tests/*` run with `PLATFORM=android` on an emulator: pass/fail set unchanged from before this plan (seeding-dependent scenarios still fail for the same real reasons).
- Electron platform-provider window-event path (`TEST_MENU_EVENT`/`TEST_OPEN_DATABASE_EVENT`) is exercised by the create/open-database scenarios under electron (these contexts are not unit tested).

## Verify
- `bun run compile` passes for all packages (desktop, desktop-frontend, user-interface, smoke-tests, mobile-frontend).
- `bun run test` passes, including the updated `test-driver`, `test-driver-ws`, and `control-bridge` unit tests.
- `bun run test:electron` (shared harness, `PLATFORM=electron`) runs the full `apps/smoke-tests/tests` suite headlessly and every scenario that passed under the old desktop harness passes now (parity).
- `bun run test:android` shows an unchanged pass/fail set.
- `grep -rn "TestControlServer" apps packages` returns nothing; `apps/desktop/smoke-tests` and `apps/desktop/smoke-tests.sh` no longer exist.
- `bun run test:all` passes (electron suite is headless, so it can be included; confirm it is wired in).

## Notes
- Screenshot is handled in-app for Electron (renderer asks main to `capturePage`, returns base64, bridge writes it) rather than host-side OS capture, because `capturePage` is reliable and window-targeted whereas host capture of an xvfb window is fragile. This is why the driver gains a `platformHandlers` hook instead of pushing screenshot entirely host-side.
- Security win: after step 12 there is no test-only HTTP server compiled into or runnable inside the shipped Electron app; the only test affordance is the renderer opening an outbound WebSocket when `testMode` + `testBridgePort` are present.
- Quit: forwarded to the app for Electron (renderer → `test-quit` IPC → `app.quit()`); the harness `electron_stop` still kills the pid as a backstop. For mobile, quit stays host-side.
- The `/create-database`, `/import-assets`, `/open-database`, `/data` endpoints on the old `TestControlServer` are not all needed: the migrated tests create/import via the real UI (menu → dialog → confirm, drag-drop), which exercises the renderer worker-pool path. Only `open-database` is kept as a driver command (window event). Confirm during step 11 that no migrated test relies on a dropped endpoint; if one does, drive it through the UI instead.
- Seeding abstraction is the key to a single suite: electron seeds the host filesystem (as today), mobile stubs seeding until `plan-mobile-storage-options.md` lands. Seeding-dependent scenarios therefore pass on electron and xfail on mobile from the same shared body — no per-platform test copies.
- `test:electron` can join `test:all` because the Electron suite runs headless; the mobile suites stay out of `test:all` (need an emulator/simulator).
- Risk: `bun run test:electron` currently bundles via `apps/desktop/smoke-tests.sh`'s `bundle_app`; `electron.sh` must reproduce that bundling (main + renderer) before launch, or the harness must call the existing bundle scripts.
