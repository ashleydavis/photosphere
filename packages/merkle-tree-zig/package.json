{
  "name": "merkle-tree-zig",
  "version": "1.0.0",
  "description": "Zig port of the merkle-tree package (only the functions needed by psi replicate and verify).",
  "private": true,
  "scripts": {
    "compile": "zig build",
    "test": "zig build test --summary all",
    "clean": "rm -rf zig-out .zig-cache"
  }
}
