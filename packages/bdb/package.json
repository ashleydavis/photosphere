{
  "name": "bdb",
  "version": "1.0.0",
  "description": "BSON database library for storing collections of documents with sharding and indexing.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "t": "bun run test",
    "tw": "bun run test:watch",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "c": "bun run compile",
    "cw": "bun run compile:watch",
    "compile": "tsc",
    "compile:watch": "tsc --watch --preserveWatchOutput",
    "clean": "rm -rf build dist coverage tsconfig.tsbuildinfo"
  },
  "keywords": [],
  "author": "ashley@codecapers.com.au",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/node": "^22.9.1",
    "jest": "^29.0.1",
    "storage": "workspace:*",
    "ts-jest": "^29.0.5",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "bson": "^6.10.3",
    "json-stable-stringify": "^1.3.0",
    "merkle-tree": "workspace:*",
    "serialization": "workspace:*",
    "storage": "workspace:*",
    "utils": "workspace:*"
  }
}
