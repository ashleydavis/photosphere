{
  "name": "encryption-zig",
  "version": "1.0.0",
  "description": "Zig port of the encryption package (only the functions needed by psi replicate and verify).",
  "private": true,
  "scripts": {
    "compile": "zig build",
    "test": "zig build test --summary all --test-timeout 20m",
    "clean": "rm -rf zig-out .zig-cache"
  }
}
