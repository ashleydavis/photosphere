{
    "name": "cli-zig",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "compile": "zig build",
        "test": "zig build test --summary all",
        "clean": "rm -rf zig-out .zig-cache"
    }
}
