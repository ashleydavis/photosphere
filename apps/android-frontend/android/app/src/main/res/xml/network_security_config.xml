<?xml version="1.0" encoding="utf-8"?>
<!--
  Network security configuration for the Photosphere Android frontend.

  Permits cleartext (ws://, http://) traffic to the smoke-test host control bridge. The app's
  own assets are served by Capacitor over http://localhost (intercepted locally), but the
  test-driver WebSocket is a real network connection, which Android blocks by default on API
  28+. The emulator reaches the host at 10.0.2.2; physical devices reach it at localhost via
  `adb reverse`. These are dev/test hosts only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
